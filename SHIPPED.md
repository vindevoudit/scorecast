# Shipped log

Detailed record of every shipped batch, newest first. Split out of CLAUDE.md on
2026-08-04, where it had grown to ~49% of the file (18k words) and was crowding out
the operational reference.

**What belongs here**: the full narrative of a shipped tier or standalone effort —
what changed, why, commit SHAs, verification performed, and operator post-deploy steps.

**What does NOT belong here**: load-bearing invariants. Those live in CLAUDE.md
"Critical considerations" so they stay in front of whoever is editing the code. If an
entry below documents a rule that must not be broken, that rule should ALSO be stated
in CLAUDE.md — this file is history, not the contract.

**Related**: [CLAUDE.md](CLAUDE.md) (working reference) · [TODO.md](TODO.md) (backlog) ·
[ARCHITECTURE.md](ARCHITECTURE.md) (deep-dive) · `tier-archive.md` in the plans archive
(at-a-glance shipped table, outside the repo).

---

## Rain-shortened cricket matches void their runs legs (2026-08-10)

Immediate follow-on from CPL auto-results. Capturing real DLS matches exposed that the runs legs
score badly on weather-shortened games, so `games.rainAffected` now voids both legs and the match
pays the flat **+50 winner alone**.

**Why voiding rather than better scaling.** A runs prediction is made pre-match and is therefore on a
20-over scale; `effectiveRuns` normalises every innings to that scale so the comparison stays
like-for-like. That works for a chase won early. It breaks under DLS, where a side chasing a _revised_
target over a _reduced_ allocation produces a total no 20-over projection can fairly be measured
against — CPL M07's 54 off 46 balls extrapolates to 141 from a sample the batting side never tried to
maximise. The obvious alternative, scaling to the actual allocation, was investigated and rejected on
three counts: it yields the literal truncated score nobody could have forecast; it is _harsher_ than
the status quo on a normal pre-match prediction (on M07, a 170/175 forecast drops from 99 runs-leg
points to 23); and it is unimplementable anyway, because the feed publishes no per-side allocation —
the two innings genuinely differ under DLS and the only mention is prose inside the status string.
Voiding is the neutral answer: nobody is rewarded for landing near an extrapolated number, nobody is
punished for a forecast the weather invalidated.

**The predicate is narrow on purpose.** `isRainShortened` requires **both** a weather/DLS marker in
the status **and** an innings genuinely cut short — under 120 balls and not all out, i.e. exactly the
case where proration would have fired for a non-dismissal reason. So a rain _delay_ that still played
its full twenty overs each scores normally (voiding there would confiscate real points), and an
ordinary chase won with overs to spare is untouched (that is the run-rate market working as designed).

**Non-retroactive by construction.** `DEFAULT FALSE` means every pre-existing row keeps normal
scoring, so no settled pick was rescored. Only matches captured or corrected after the migration can
carry the flag.

**Surfaces.** Written in step 1's transaction alongside the innings columns (same reason: `setResult`'s
FOR UPDATE re-read must observe it before `applyPickTransition` recomputes, or toggling the flag would
recompute identical points and hit the idempotency short-circuit). Mirrored into `src/utils/scoring.js`
and `scripts/backfill-user-scores.mjs`, and both the backfill's raw SQL projection and `StatsService`'s
`attributes` list gained the column — the same trap `games.sport` documents. The admin form gains a
checkbox seeded from the game, so correcting an auto-captured rain match does not silently un-void it;
the pick panel states the rule before the user types; and the post-match scorecard shows
`voided (rain)` instead of a 20-over equivalent that played no part in the score.

**Verification.** 326 unit tests (11 new across `isRainShortened`, payload stamping, and the scoring
branch) and 25 cricket E2E (3 new: a real DLS shape voiding to 50, an admin un-void restoring the
legs, and a rain delay correctly _not_ voiding).

## CPL auto-results — production rollout (2026-08-18)

Both cricket batches went live on CD run `32088441572` (commits `2727d89` + `86622f4`). Sequence, and
what each step proved:

1. **Push → CD green.** Build, migrate job and rollout all succeeded; both migrations applied. First
   confirmation came from the public API: M01 came back `resultSource: "admin"`, i.e. the
   `20260810000001` backfill correctly froze the one hand-entered result out of the automation.
2. **Key Vault + env.** `cricapi-api-key` seeded (via `--file` so the value never hit a command line),
   then wired as a KV-referenced Container App secret plus `CRICAPI_SERIES_ID`. All five pre-existing
   secrets survived the `secret set`. `CRICKET_RESULT_WRITE_ENABLED` deliberately left unset.
3. **Two temporary overrides, still in shadow.** No CPL match fell inside the 12h eligibility window
   that day and `providerMatchId` was null everywhere, so nothing would have been observable. Setting
   `CRICKET_MATCH_RESOLVE_CRON='*/5 * * * *'` and `CRICKET_CAPTURE_LOOKBACK_HOURS=720` made the whole
   backlog visible _without_ enabling writes — which turned the rollout's riskiest moment into a
   readable dry run over the exact rows about to be touched.
4. **Resolver: 35/35 stamped**, 0 unresolved, 4 playoff placeholders, 0 `needsManualEntry`, 0
   `stranded` — identical to the local run.
5. **Prod shadow review.** Seven payloads logged in full. Every "won by N runs" margin matched the run
   difference exactly; both DLS matches (M02, M07) were correctly inverted against their run totals and
   flagged `rainAffected`; both malformed-label matches (M03, M05) recorded
   `malformed-inning-label-resolved-by-elimination`. One match (M09) refused with `no-innings` because
   the job had exhausted its own 10/min burst budget on `match_info` calls — it degraded exactly as
   designed, logging and deferring rather than writing a partial scorecard.
6. **Writes enabled** (with `CRICAPI_RATE_LIMIT=30` for the catch-up). Next tick: **8 written, 0
   refused, 0 shadowed, 0 unmapped**, budget 66/100. All nine played matches now carry results — 8
   `auto`, 1 `admin`, 3 rain-voided.
7. **Overrides removed**, steady state verified: resolver back on `20 6 * * *` reporting all-clean, and
   the results job ticking in ~77 ms with no output, i.e. the cost gate closed as intended.

**Operational note for the next backlog.** Each capture costs 1 `series_info` + 1 `match_info`, against
a default 10/min burst budget. Clearing 8 at once trips it and the tail defers a tick (self-healing, so
harmless). Raise `CRICAPI_RATE_LIMIT` for the run if you want it in one pass. Steady state — one match a
day — never comes close.

## CPL auto-results — automatic T20 result capture from CricketData.org (2026-08-10)

Tier 34 shipped cricket with no feed at all: fixtures from a committed JSON file, and **every result typed
into an admin form by hand** — eight numbers plus a winner, 39 times a season. This removes the typing.
Fixtures stay on the committed file; only results are automated.

**Choosing a provider was the hard part, and it eliminated most of the market.** Against one requirement —
CPL coverage at hobby-app price — **Sportmonks does not carry CPL at all** despite 140+ cricket leagues, and
neither does AllSportsAPI. **Sportradar Cricket v2 is the official CPL data partner** but is an enterprise
contract. Roanuz is ~$200/mo, EntitySport $250/mo for domestic T20, Goalserve $125/mo. That left
**CricketData.org (CricAPI)** — free 100 hits/day, $5.99/mo for 2,000 — as the only viable option, and its
payload happens to fit exactly: `score: [{r, w, o, inning}]` where `o` is already the `18.4` over-notation
`lib/sports.js oversToBalls` parses. Its CPL coverage is not documented publicly, so **Step 0 of the rollout
is a signup plus one `GET /v1/series?search=Caribbean`**; fallbacks if that comes back empty are a Sportradar
trial or Cricsheet's free post-match files (~24h lag).

**Shape.** Five new modules, one migration, two cron jobs, one committed alias file, one operator script.
`lib/scoring.js`, `GameService`, `LeagueService`, `validation/schemas.js` and every football job are
untouched. The automatic path terminates in the **unmodified** `CricketResultService.setCricketResult`, so it
inherits that service's load-bearing "scorecard first, then `GameService.setResult`" ordering — and with it
the Tier 24 dual-writer, notifications, badges, streaks and leaderboard invalidation — rather than
reimplementing a write.

**Every judgement is a pure function.** `lib/cricketResult.js` has no DB and no network, so the four genuinely
hard derivations are unit-testable like `lib/scoring.js`: the two-signal winner (run totals _and_ the status
string, because **DLS breaks run-total comparison** — a rain-reduced chase wins on a revised target with fewer
runs), sides-assigned-by-name (batting order is toss-dependent, so `score[0]` is not the home side), the
`allOut` matrix (9 down in a short first innings is genuinely undecidable and refuses the whole match), and
match resolution. The governing rule throughout is **refuse rather than guess** — the admin form still works,
so a match needing manual entry is a minor annoyance while a match scored backwards is a leaderboard nobody
trusts.

**Safety is three layers.** `games.resultSource` (`NULL` / `'auto'` / `'admin'`) makes the claim a conditional
`UPDATE ... WHERE resultSource IS NULL`, settling the admin-vs-cron race in the database rather than in JS; the
`'admin'` default on `setCricketResult` means the existing admin route locks the automation out of any
corrected game without a line changing. The derived payload is validated against the **same `cricketResultSchema`**
the admin form obeys. And `CRICKET_RESULT_WRITE_ENABLED` defaults to **shadow mode**, logging the exact payload
it would write while touching nothing.

**Verification.** 72 new unit tests (`cricketTeamNames`, `cricketResult`, `cricketMatchResolution`) — full unit
suite 310/310. 8 new E2E tests in `tests/e2e/api/cricket.spec.js` covering auto-capture end-to-end with real
point movement, idempotency, the admin-override lock, abandonment, refusal on an unmappable name, refusal on a
tie, and shadow mode — cricket spec 22/22, full Playwright suite 464 passed (one unrelated admin-panel flake
that passes in isolation). The migration was exercised against a `sequelize.sync()`-first schema, which is the
CI-parity check.

**Live validation, same day.** The provider gate passed: CricAPI carries CPL as
`Caribbean Premier League 2026` (`929c36f6-…`), 39 matches, 2026-08-07 → Sep 20, an exact match for the
committed fixture file. A full local shadow run then resolved **35/35 non-playoff fixtures** with zero
ambiguity (the other 4 are the playoff placeholders, correctly refused) and derived **9/9 finished matches**
cleanly — 7 `agreed`, 2 `dls`, all schema-valid, zero refusals. **Zero alias entries turned out to be needed**;
the normaliser reconciles every 2026 side on its own, and the three pre-seeded rebrand entries never fire.

Three things the live feed corrected, all folded back into the code and its invariants:

- **`series_info` never carries `score[]`** (9 ended matches, 0 with innings), so the `match_info` fallback is
  the scorecard path, not an edge case — one extra hit per capture.
- **`dateTimeGMT` is skewed** — uniformly +4 h for most fixtures, up to 20 h for the eight recorded as a 20:00
  local start. The resolution window moved 14 h → **24 h**; a 20 h window already gave exactly one candidate
  for all 35 fixtures and stays unambiguous to 48 h, so 24 h sits mid-band.
- **A malformed innings label** — both team names concatenated
  (`"Antigua and Barbuda Falcons,Saint Lucia Kings Inning 1"`) on 2 of the first 9 matches. Now resolved by
  **elimination**: a T20 side bats exactly once, so with two innings and one cleanly mapped, the other is
  determined. Kept narrow (exactly 2 innings, exactly 1 clean mapping, and the bad label must mention the side
  being assigned), and a mis-elimination is still caught downstream by the winner cross-check.

**The DLS design earned its keep immediately.** Two of the first nine matches — M02 (`won by 19 runs (DLS
Method)`, 109 vs 94) and M07 (`2nd innings reduced to 8 overs due to rain, DLS target 52`, 54 vs 98) — would
have been scored **backwards** by naive run-total comparison, inverting the flat +50 for everyone. A ~22%
incidence in week one.

**Operator steps.** (1) Seed Key Vault `cricapi-api-key` **before** the Bicep reapply; set
`CRICAPI_SERIES_ID=929c36f6-9ed6-4cec-a2ad-910a2ee4f701`. (2) Deploy + `npm run db:migrate`, leaving
`CRICKET_RESULT_WRITE_ENABLED` unset so prod starts in shadow. (3) Confirm one shadow tick, then flip the flag.
(4) **Clear the backlog**: the gate only reaches 12 h back, so set `CRICKET_CAPTURE_LOOKBACK_HOURS=720`, let one
tick run, then remove it. (5) **Rename the four playoff rows** in the admin game editor once seeding is known —
they cannot auto-capture while they read "Winner of Qualifier 1".

## Tier 34 — T20 cricket (Caribbean Premier League) multi-sport expansion (2026-08-05)

**Tier 34** (`f67ac5f`, `c736a5b`, `318a694`, `963a724`, `0b85c33`, `321f927`, `05d8965`, `<docs>`) — added
T20 cricket alongside football, under a hard constraint that **nothing in the existing football scope may
change behaviour**. Football is live and mid-season; that constraint, not the cricket features, drove the
structure.

**The market.** Winner is a flat **+50** (deliberately not odds-weighted — cricket has no probability model,
and its games sit on the `0.5 / 0 / 0.5` sentinel permanently). Each optional runs prediction pays
`max(0, 100 - |effective - predicted|)`, so a match is worth up to **250** against football's ~100. A side
that did not face 20 overs is scaled to a **20-over equivalent unless it was bowled out**. Both causes of a
short innings prorate — a chase finishing early as well as rain — so 130 off 80 balls counts as 195, and
predicting the literal score of a successful chase pays 35 rather than 100. Intended, and locked by a test.

**How isolation was achieved.** Four techniques: a guard at the top of `scorePick` (football body
byte-identical below); wrapping rather than extracting the GameCard market JSX; a parallel
`SportLeaderboardService` rather than a third condition in the existing read paths; and composing with the
unmodified `GameService.setResult` rather than extending it. `setResult`, `bulkSetResult`, `applyLiveUpdate`,
`updateGame`, the football result route and its schema, and the shared E2E fixtures were all left untouched.

**Proof.** The full Playwright suite went from 443 to **457 passed, 1 skipped, 0 failed**, with **no football
spec modified** — the 14 new tests live in a self-seeding `tests/e2e/api/cricket.spec.js`. 34 new unit tests
(238 total). An end-to-end check on a real CPL fixture scored 222, then a scorecard correction with the
result unchanged moved the materialized total by exactly +7 with counters unmoved.

**Bugs found while tracing and fixed here.** `StatsService`'s `Game` projection listed neither `sport` nor
the score columns, which would have made `scorePick` take the football branch and silently drop both runs
legs — the stats dashboard disagreeing with the leaderboard forever. `scripts/backfill-user-scores.mjs`
carries a fifth copy of the scoring formula (CLAUDE.md documented two); a re-run would have wiped every runs
point from `user_scores`.

**Bugs found and deliberately deferred**, because fixing them changes football behaviour: the
`LeaderboardService` `new Map(...)` collapse that already truncates points for any league with two seasons;
`pickStatus` having no void branch, so a postponed football game reads "Live" inside Completed forever; and
result notifications re-fanning-out on every correction. All three are in TODO.md.

**Data.** The full 39-match CPL 2026 schedule (7 Aug – 20 Sep) is committed as
[data/cpl-2026-fixtures.json](data/cpl-2026-fixtures.json), sourced from the CPL fixture announcement and
cross-checked line by line against Wikipedia. Times are stored local-to-venue with an explicit per-fixture
UTC offset, because every Caribbean venue is UTC-4 year-round **except Sabina Park in Jamaica, which is
UTC-5** — the one detail most likely to be got wrong.

**Operator steps.** (1) Run the migrations. (2)
`node scripts/import-cricket-fixtures.mjs data/cpl-2026-fixtures.json` against the production
`DATABASE_URL` — idempotent, `--dry-run` first. (3) Rename the four playoff placeholder slots through the
admin game editor once the league table settles. (4) Enter each result through admin → Games → Enter result.

---

## World Cup Aftermatch — per-user Wrapped-style recap (2026-07-21)

**World Cup Aftermatch — per-user Wrapped-style recap** (2026-07-21). A Spotify-Wrapped-style, **self-only** retrospective of a user's World Cup prediction run — a full-screen tappable **story** (one big animated stat per slide: predictions made, points, accuracy, boldest call, team of the tournament, upsets called, deepest stage run, overall percentile, a templated "prediction personality" archetype) ending on a shareable 1080×1920 image that includes the **boldest upset + the match it came in**. The user-facing name is **Aftermatch**; ALL code identifiers keep the `wrapped` name (service, `/api/me/wrapped` route, view id `'wrapped'`, `src/components/wrapped/*` files) for contract stability — the rename is copy-only. **Backend**: new [services/WrappedService.js](services/WrappedService.js) `getWrappedForUser(userId)` — resolves the WC league (`sourceLeagueId='WC'`), loads all WC games + picks, scores with `scorePick`, reuses TrophyService's exported `rankAmong`/`topPercentOf` + `lib/stages.js` `stageLabel`/`medalFor`; pure helpers `findBoldestCall` (lowest-prob winning pick) / `teamOfTournament` (most-backed nation) / `countUpsets` (correct picks < 0.33) / `buildArchetype` (Oracle/Daredevil/Analyst/Optimist, `<3` scored → Newcomer; axes = win rate + mean `1 − picked-side-prob`) exported for tests; on-the-fly + 5-min cache (`wrapped:<userId>`, skipped in test), NOT materialized; `hasData:false` when no scored WC picks. New `GET /api/me/wrapped` in [routes/me.js](routes/me.js) (auth, scoped to `req.user.id`, **self-only — no per-username surface**, mirrors `/me/stats`). **Frontend**: lazy [src/views/WrappedView.jsx](src/views/WrappedView.jsx) launch tile → [src/components/wrapped/WrappedStory.jsx](src/components/wrapped/WrappedStory.jsx) (config-driven `SLIDES` that auto-skip empty stats, `AnimatePresence` transitions, count-ups via `animate`+`statsCountUp`, segmented progress bars + tap/swipe/arrow nav, **raw** `useReducedMotion` from `motion/react` so motion stays on mobile) → [WrappedShareCard.jsx](src/components/wrapped/WrappedShareCard.jsx) (inline-styled Orbitron, includes the boldest upset + its match) shared via [shareWrapped.jsx](src/components/wrapped/shareWrapped.jsx) (createRoot capture + `document.fonts.load` Orbitron gate + `captureNodeToPng` + `shareBlob`, GameCard `captureAndShare` template). Registered via `ICONS.wrapped` ([src/components/Sidebar.jsx](src/components/Sidebar.jsx)), `{ id: 'wrapped', label: 'Aftermatch' }` + view switch ([src/views/DashboardView.jsx](src/views/DashboardView.jsx)), and `'wrapped'` added to `DEEP_LINK_ALLOWED_VIEWS` ([src/contexts/DataContext.jsx](src/contexts/DataContext.jsx)). **Verification**: `npm run test:unit` 204/204 (+14 new `tests/wrappedService.test.js`), `npm run build` clean (`WrappedView` chunk ~5.4 KB gzip + `WrappedShareCard` split for the capture path), Playwright `api/me.spec.js` 38/38 (+3: anon 401, `hasData:false` shape, `hasData:true` via `createWcCabinetFixture`), lint clean on touched files (pre-existing `marketing/commercial/*` untracked-file errors are unrelated). **Operator post-deploy**: none — additive read-path, no migration; the story populates once a user has settled World Cup picks. Plan file: `C:\Users\vinde\.claude\plans\make-a-plan-for-peppy-orbit.md`.

---

## Trophy Cabinet — World Cup per-stage placements (2026-07-06)

**Trophy Cabinet — World Cup per-stage placements** (2026-07-06; local, not yet committed). A per-user, per-stage World Cup retrospective: for each tournament segment (Group Stage / Round of 32 / Round of 16 / Quarter Finals / Semi Finals / Third Place / Final) it shows the player's **placement + percentile (`Top X%`) + podium medal**, both OVERALL (among everyone who picked in that stage) and within their ScoreCast social groups, topped by a medal showcase (gold/silver/bronze tally + best finish). Surfaced as a new `🏆 Trophy Cabinet` **sidebar view** (authed-only) AND a **Cabinet sub-tab on any profile** (visible-on-any-profile, gated by the existing profile-visibility rule). **Foundation**: football-data.org returns a `stage` token per match that was previously discarded — new `games.stage VARCHAR(32) NULL` column (migration [20260706000001-games-add-stage.js](migrations/20260706000001-games-add-stage.js), raw SQL `ADD COLUMN IF NOT EXISTS` per the CLAUDE invariant) populated by [lib/footballApi.js](lib/footballApi.js) `normalizeFixture` (`stage: raw.stage || null`) + [services/LeagueService.js](services/LeagueService.js) `upsertFixture` (stamped into `baseAttrs` so the update path's `Object.assign` **backfills all existing WC games on the next sync** — no backfill script). **Backend**: new [lib/stages.js](lib/stages.js) (WC_STAGE_ORDER + STAGE_LABELS + `stageLabel` title-case fallback + `medalFor`) + [services/TrophyService.js](services/TrophyService.js) `getCabinet(target, viewer)` — resolves the WC league (`sourceLeagueId='WC'`), loads WC games + all picks, per stage builds `pointsByUser` via `scorePick`, computes the target's competition rank (`rankAmong` = 1 + count strictly greater) + `topPercentOf` (clamped [1,100]) + medal, then per visible group re-ranks among members. Computed on-the-fly, cached 5 min via [lib/cache.js](lib/cache.js) (`trophy:<targetId>:v:<viewerId|anon>`, skipped in test); **deliberately NOT materialized** (the Tier 24 `user_scores` tables have no stage axis; the WC is bounded so a cold-path scan is sub-second — don't wire it into the dual-writer). Route `GET /api/users/:username/trophy-cabinet` ([routes/users.js](routes/users.js)) reuses the now-exported `UserService.canViewProfile` gate (same-shape 404 for private/friends-gated); payload reveals only the subject's own rank numbers so no masking needed; the group section shows self/admin all the target's groups but everyone else only SHARED groups (no private-membership leak). **Frontend**: [src/utils/stages.js](src/utils/stages.js) (display mirror — keep in sync with lib/stages.js), lazy-loaded [src/components/TrophyCabinet.jsx](src/components/TrophyCabinet.jsx) (shared by the sidebar [src/views/TrophyCabinetView.jsx](src/views/TrophyCabinetView.jsx) + the ProfileView Cabinet sub-tab; own Vite chunk), `ICONS.trophy` in [src/components/Sidebar.jsx](src/components/Sidebar.jsx), a new `{ id: 'trophy' }` tab + view switch in [src/views/DashboardView.jsx](src/views/DashboardView.jsx), and `'trophy'` added to `DEEP_LINK_ALLOWED_VIEWS` in [src/contexts/DataContext.jsx](src/contexts/DataContext.jsx). **Verification**: `npm run test:unit` 190/190 (+20 new `tests/trophyService.test.js` cases), lint clean, `npm run build` clean (new `TrophyCabinet` + `TrophyCabinetView` chunks), Playwright `api/users.spec.js` 25/25 (+5 new cabinet cases incl. an end-to-end WC-game placement assertion, private→404, anon→200), `api/games.spec.js` + `api/leaderboard.spec.js` 34/34, `pick-and-result.spec.js` 1/1, and a driven UI smoke (sidebar tab → World Cup heading → Group Stage card → best-finish + placement rendered) that was then removed. Migration roundtrip up→undo→up clean. **Operator post-deploy**: after the migration lands, **Sync the World Cup league once** (admin → League Manager → Sync, or wait for the 03:00 UTC `syncFixtures` cron) so every WC game gets its `stage`. Confirm the actual upstream stage tokens by inspecting a synced WC game (verifies `LAST_32` for the 48-team bracket) — `stageLabel` handles any surprise gracefully. Plan file: `C:\Users\vinde\.claude\plans\i-want-to-add-silly-hare.md`.

---

## World Cup knockout odds auto-fill on sync (2026-06-27)

**World Cup knockout odds auto-fill on sync** (2026-06-27; commit `ebeef4d`, deployed via CD run `28289640471`). World Cup knockout fixtures (Round of 32, R16, QF, SF, Final) were stuck at the `(0.50, 0.00, 0.50)` sentinel: the result-capture cascade ([services/PredictionService.js](services/PredictionService.js) `rePredictFutureFixtures`) only re-predicts when a team's Elo changes, but a knockout team plays no match between qualifying and the fixture it advances into — so once football-data.org resolved a slot's placeholder names (`TBD` / `Winner Group A` → real nation), nothing ever computed its odds. Three-part fix. **(1)** New [lib/placeholderTeam.js](lib/placeholderTeam.js) — backend mirror of [src/utils/teamNames.js](src/utils/teamNames.js) `isPlaceholderTeam` (the `runner-up` token was added to both regexes this commit). **(2)** [services/PredictionService.js](services/PredictionService.js) `rePredictFutureFixtures` now skips any fixture with a placeholder side — a structural guard so a still-`TBD` slot keeps the sentinel (frontend gate shows "Picks open once both teams advance") and a stale auto-inserted placeholder team row can't resolve to a real Elo and emit wrong odds (no longer relies on the TBD team row being deleted by `fixup-wc-state.mjs`). **(3)** [services/LeagueService.js](services/LeagueService.js): `upsertFixture` stops calling `ensureTeamExists` for placeholder names (no more `TBD`/`Winner …` pollution in the `teams` table) and returns `matchupChanged` (captures prior `homeTeam`/`awayTeam` before `Object.assign`; new scheduled fixture = changed); `syncFixtures` then fires `rePredictFutureFixtures` POST-COMMIT for the union of (a) teams on fixtures whose matchup changed this sync and (b) teams on any scheduled fixture still at the exact sentinel (`homeProbability=0.50 AND drawProbability=0 AND awayProbability=0.50` — self-heals already-resolved-but-never-predicted fixtures on the next sync). Best-effort + lazy-require'd PredictionService (dodges require-cycle) + try/catch so a model-load failure never fails the sync; returns `rePredicted` in the summary. Fires on BOTH the daily `syncFixtures` cron AND the admin **League Manager → Sync** button. Sentinel match is exact because a genuine prediction is always nudged off `(0.50, 0.00, 0.50)` by [lib/ml/normalize.js](lib/ml/normalize.js) — only ever catches never-predicted rows, leaves manual admin edits untouched. **Verification**: lint clean (3 pre-existing `src/components` warnings unchanged), `npm run test:unit` 170/170 (+3 new `tests/placeholderTeam.test.js` cases), `npm run build` clean, CD green (build → migrate (none) → roll out → `/healthz` 200; live `bantryx.com/healthz` returns `{"ok":true}`). **Operator post-deploy**: to fill the currently-stuck R32 odds immediately instead of waiting for the 03:00 UTC cron, click **Sync** on the World Cup league in admin → League Manager; watch for the `syncFixtures: re-predicted resolved fixtures` log line. Fixtures whose teams are still `TBD` upstream fill automatically on whichever later sync first sees real names. No migration. ARCHITECTURE.md §8.17 ("Sync-time re-prediction hook") + intl-model rollout runbook updated.

---

## Leaderboard Friends-tab regression fix (2026-06-13)

**Leaderboard Friends-tab regression fix** (2026-06-13). After the marketing launch pushed the user base past 50, the LeaderboardView **Friends** sub-tab — which was a client-side filter over `leaderboard.overall` (only the top-50 slice from `getOverallSlimForViewer`) — silently dropped any friend, and the viewer themselves, sitting below rank 50; commit `68e972a` (`f.id`→`f.userId`) was correct but only surfaced friends inside the top 50 and also made friend rows interleave into the **Overall** compact view with "… N more players" dividers (the clutter the user reported). Fix: a dedicated server-side friends block. [services/LeaderboardService.js](services/LeaderboardService.js) gains `getForFriends(viewerId, {leagueId, seasonId})` + `getForFriendsForViewer(viewer, opts)` (viewer + every accepted friend via `getViewerFriendIdSet` + self, read from the materialized `user_scores`/`user_scores_overall` tables — same branch as `getForGroup` — sorted points DESC, masked per the Tier 8.6 / `listFriendsPicks` contract: self never masked, `private` friends masked, no `exemptIds`; **uncached** — per-viewer set + sub-ms reads). [routes/leaderboard.js](routes/leaderboard.js) adds a `friends` array to the response (anon → `[]`). [src/contexts/DataContext.jsx](src/contexts/DataContext.jsx) stores `leaderboard.friends`. [src/views/LeaderboardView.jsx](src/views/LeaderboardView.jsx): Friends sub-tab renders the server block (so off-top-N friends + self always show), empty state gated on the friend set not `entries.length`; Overall sub-tab no longer passes `friendUserIds` (clean global standings) and now passes `viewerRow={overallMeta.viewerRow}`. [src/components/LeaderboardCard.jsx](src/components/LeaderboardCard.jsx) gains a `viewerRow` prop that renders a pinned "Your position" row when the viewer is below the loaded slice (mirrors `GroupLeaderboardCard`). Groups tab was already correct (server-side block + off-page viewer row) — untouched. **Verification**: lint 0 errors (3 pre-existing `src/components` warnings unchanged), `npm run test:unit` 167/167, `npm run build` clean, Playwright `api/leaderboard.spec.js` (+3 new friends tests), `leaderboard-view.spec.js` 4/4 (+1 new friend-row test; fixed a stale empty-state heading selector that only ever passed via the old self-row branch), `leaderboard-scoring.spec.js`, `pick-and-result.spec.js` all green. **Operator post-deploy**: none — additive read-path change, no migration. Plan file: `C:\Users\vinde\.claude\plans\the-leaderboard-behavior-is-purrfect-pizza.md`.

---

## Tier 31 — Matchday graphics automation (2026-06-11)

**Tier 31 — Matchday graphics automation** (2026-06-11; local, not yet committed). The marketing kit could render the four live-fixture graphics (`kickoff-countdown` / `halftime` / `fulltime` / `picks-vs-model`) but only via the manual `npm run assets:marketing` CLI. This adds an in-container cron that auto-renders the right graphic per active-league match at the right moment and **emails** the PNGs to the operator (no IG/TikTok story API exists for auto-posting). **(1) Shared renderers**: the four renderers + `rasterize` + `loadFonts` + `topMark`/`countdownParts`/`fitOrbitron` helpers were lifted out of the CLI into new [marketing/lib/render.mjs](marketing/lib/render.mjs) (ESM, DB-free, qrcode-free); the CLI now imports them (kept `renderThankYou` + the 29 deterministic core renderers + samples). Output is **byte-identical** — verified by an offline regen showing zero tracked-PNG diffs. **(2) Prod-image bundling**: `@resvg/resvg-js` moved devDeps → deps (package-lock dev-flags flipped via `npm install --package-lock-only`); [Dockerfile](Dockerfile) runtime stage COPYs `marketing/lib` + `marketing/fonts` (not `out/`). **(3) Idempotency table**: new `marketing_posts (gameId, type)` compound-PK table — [migration 20260611000001](migrations/20260611000001-create-marketing-posts.js) (raw SQL `IF NOT EXISTS`, FK `gameId → games(id) ON DELETE CASCADE`) + [models/MarketingPost.js](models/MarketingPost.js) + association in [models/index.js](models/index.js). **(4) Email attachments**: [lib/email.js](lib/email.js) `send()` gained an optional `attachments: [{filename, content}]` arg (Buffer/base64 → Resend; log-mode logs filenames only; key only set when present so existing sends are unchanged). **(5) The job**: [lib/jobs/postMatchdayGraphics.js](lib/jobs/postMatchdayGraphics.js) — every 5 min, four tight indexed "due" SELECTs (`countdown` kickoff in [now+1h55m, now+2h); `picks-vs-model` kickoff in [now+5m, now+10m), skip placeholder/sentinel/crowd-0; `halftime` in-progress + halfTimeReached + elapsed [45,65]min; `fulltime` finished+result+scores with a **5h kickoff cold-start guard** since `games` has `timestamps:false`), each filtered against `marketing_posts`. Renders square+story buffers, batches one email per type per tick (subject `⚽ Matchday: <type> — N matches`), stamps `marketing_posts` only AFTER a successful send (or dev log-mode). Renderer + fonts dynamic-`import()`ed once and cached; idle ticks never load them. Fetches via app models + `GameService.getCrowdForGames` (NOT `livedata.mjs`). **(6) Registration + config**: [server.js](server.js) registers it only when `MARKETING_AUTOMATION_ENABLED` is `'1'`/`'true'`; the job no-ops without `MARKETING_EMAIL_TO`. New env (`MARKETING_AUTOMATION_ENABLED`, `MARKETING_EMAIL_TO`, optional `MARKETING_GRAPHICS_CRON`) documented in [.env.example](.env.example) + threaded through [infra/main.bicep](infra/main.bicep) → [infra/modules/app.bicep](infra/modules/app.bicep) as two new optional params (default empty = disabled, safe on reapply). **Verification**: full `npm run lint` clean (0 errors; 3 pre-existing `src/components` warnings unchanged); migration roundtrip up→undo→up clean with PK `(gameId, type)` + FK `confdeltype='c'` confirmed; a staged dry-run (one game per window) rendered + stamped all four types, run #2 returned `{skipped:true, reason:'nothing-due'}` (idempotent), cascade cleanup left 0 orphan rows; a **real send** delivered four emails (countdown/picks-vs-model/halftime/fulltime, each square+story) to the operator inbox via the verified `noreply@bantryx.com` sender, all `delivered:true`. **Operator post-deploy**: after CD lands the migration, set `MARKETING_AUTOMATION_ENABLED=1` + `MARKETING_EMAIL_TO=<inbox>` on the Container App (Bicep params or `az containerapp update --set-env-vars`); the cron self-registers on next boot. To force an immediate send for testing: `node -e "require('./lib/jobs/postMatchdayGraphics').run()"`. Plan file: `C:\Users\vinde\.claude\plans\in-the-marketing-section-lovely-puddle.md`.

---

## Tier 30 Phase 3 polish + Phase 5 Chunk 5.1

**Tier 30 Phase 3 polish + Phase 5 Chunk 5.1** (15 commits 2026-05-31 → 2026-06-01; covers the post-C1 UX polish wave + the four worthwhile P1 items from Phase 5). **Phase 3 chrome reorg** (commits `04c7cfc` / `6dba567` / `851c61c` / `498928f` / `7fcba2f` / `5c9cea9`): NotificationBell glyph swapped from 🔔 emoji to an inline Lucide-style stroke `<BellIcon>` so the bell shares one icon language with the GameCard Share/Undo glyphs; Undo button on GameCard gains a `border border-default bg-overlay/40` chip shell matching Share (danger-tone hover preserved); new [src/components/RefreshButton.jsx](src/components/RefreshButton.jsx) restyled to the same `h-12 w-12 rounded-3xl bg-overlay text-accent` shell as NotificationBell so the two icon-only squares read as a matched pair. DashboardView mobile chrome restructured into a 2-row layout: row 1 = `grid grid-cols-[1fr_auto_1fr]` with `justify-self-start` hamburger / center brand / `justify-self-end` UserMenu (true viewport-center BANTRYX regardless of asymmetric side-cell widths); row 2 = `Refresh | min-w-0 flex-1 Search | Bell`. NotificationBell collapses to icon-only on every viewport. The third row from the prior layout dropped. Copy: "Payout locks in at kickoff" → "Points allocation locks in at kickoff" on PayoutMatrix; PicksHistory blurb stripped of apostrophe + semicolons ("Switch between your own picks and friend picks. Filter by status or scope."); SearchBar placeholder shortened to "Find users, groups..." so it fits at 360px without triggering iOS Safari's input-zoom on focus (the 16px floor in `src/index.css` remains, but the placeholder text itself is what was overflowing). Profile sub-tab "Overview" renamed to "Summary" + ProfileView stats grid converted to `sm:grid-cols-2 lg:grid-cols-5` adding a "Best streak" tile reading `user.longestWinStreak` (numerals in `.font-led` Orbitron tabular-nums for consistency with the Stats sub-tab). [services/UserService.js](services/UserService.js) `getProfileByUsername` payload extended to include `streak: { current: target.currentWinStreak || 0, longest: target.longestWinStreak || 0 }`. [src/components/SubTabs.jsx](src/components/SubTabs.jsx) tightened for narrow viewports — `gap-0.5 p-0.5 sm:gap-1 sm:p-1` on List + `px-2 py-1 text-xs sm:px-3 sm:py-1.5 sm:text-sm` on Trigger so 4-tab Settings + 3-tab Profile sub-tab rows fit on iPhone SE (375px). SettingsView abbreviates "Appearance"→"Theme" and "Notifications"→"Alerts" on `<sm:` via duplicate-span pattern (`<span className="sm:hidden">Theme</span><span className="hidden sm:inline">Appearance</span>`) so 4 sub-tabs fit at the narrowest viewport. UserMenu trigger ditched the inline StreakFlame chip; flame chip moved INSIDE the dropdown's `DropdownMenuLabel` next to the username so the top-bar trigger reads cleanly. **Phase 3 share card redesign** (commits `5c9cea9` / `d039d9a`): GameCard's Share button defaults to **9:16 story format** (was square); ShareableCard iterated through several layouts and reverted to the "i liked the old design better" original with three polish layers — (1) drop the UPCOMING/SCHEDULED kicker; (2) symmetric centered top-glow blob at `top:-260px left:50% transform:translateX(-50%) width:900px` instead of off-axis top-right (closes the "dark bar on right edge" perception); (3) 6 flex children (BANTRYX wordmark → date line → matchup → outcome [thin "I picked" kicker + bold cyan-glowing team] → tagline [NO BETTING / JUST BANTRYX] → footer) with `justify-content: space-between` so vertical rhythm is even instead of leaving a void at the bottom. Team-name wrap: `overflowWrap: 'break-word'; wordBreak: 'normal'; hyphens: 'manual'` + size reduction so "Manchester United" wraps at the space, not mid-word. Inline match-date line formatted via `Intl.DateTimeFormat` → `"31 MAY 2026 · 18:00"`. **`d039d9a` fixed the "card renders in Courier New not Orbitron" bug** — the previous capture flow waited 2 `requestAnimationFrame` ticks (paint settled) before calling html-to-image's `toBlob`, but **2 RAFs cover layout/paint, not web-font download**. The browser was still rendering Orbitron-targeted text in the fallback Courier New when toBlob rasterized. Fix: between the RAF settle and the toBlob call, explicitly `await Promise.all(document.fonts.load("<weight> <size> 'Orbitron'"))` for every weight the ShareableCard uses (500/600/700/800/900), then `await document.fonts.ready` as a safety net. Also added `@fontsource/orbitron/latin-500.css` + `latin-900.css` imports in [src/main.jsx](src/main.jsx) so the kicker (500) and BANTRYX wordmark (900) render at their real weights instead of faux-bolding off 600/800. Story-button dropped from the action row — the iOS `instagram-stories://share` URL scheme requires native `UIPasteboard` writes unreachable from a PWA; native share sheet on mobile + PNG download on desktop is the only realistic path. **Phase 3 cleanup** (commits `40a8f2d` / `0c3aed6`): GameCard had 3 stacked `border-t` rules between FriendPicksPanel and CommentThread (FriendPicksPanel's own border + a redundant wrapper around CommentThread + CommentThread's own internal `mt-4 border-t pt-4`) — ~40px of dead-space gap. Dropped the middle wrapper; CommentThread now renders bare right after FriendPicksPanel and self-separates with its internal border. Sidebar icon semantics fixed — [src/components/Sidebar.jsx](src/components/Sidebar.jsx) `ICONS.groups` was a two-overlapping-busts glyph that was indistinguishable from the (missing-and-falling-back-to-profile) `friends` icon; swapped Groups to a 5-node network graph (4 corner circles + 1 hub with diagonal spokes) reading as "interconnected community", and `friends` inherits the bust-pair reading as "close 1:1 ties". **Phase 5 Chunk 5.1 polish** (commit `50e48c5`): 4 worthwhile Tier-26 P1 items from the plan; the other 11 audited and skipped — already done (P1-6/10/11), functionally correct (P1-1/3/7/8/13), or feature-sized beyond polish (P1-2/12/15). **P1-4**: [services/GroupService.js](services/GroupService.js) `listJoinRequests` swapped its `for (const row of rows) await getUserById(row.requesterId)` loop for a single batched `User.findAll({where: {id: {[Op.in]: requesterIds}}, attributes: ['id','username','displayName']})` + `Map`-based hydration. N pending requests was N sequential round-trips; now 1 indexed-PK SELECT regardless of size. **P1-5**: [services/CommentService.js](services/CommentService.js) `fanOutGroupComment` replaced naive `Promise.all(recipients.map(notify))` with an 8-at-a-time worker-pool drainer pattern. At MAX_GROUP_MEMBERS=2000 (Tier 22 M4), naive fan-out would burn the 20-slot Sequelize pool (Tier 25 A1) and thunder the Web Push transport. Worker pool preserves the fire-and-forget caller timing (function still resolves only when every notification has been attempted) while bounding parallel notify() calls so other request handlers + cron jobs aren't pool-starved. **P1-9**: [src/contexts/DataContext.jsx](src/contexts/DataContext.jsx) user→null cleanup useEffect extended to also reset `gameFilters` + `leaderboardFilters` to `{leagueId: '', seasonId: ''}`. Without this, user A's chosen league/season filter persisted into user B's views after a shared-browser swap. **P1-14**: [src/components/LoginForm.jsx](src/components/LoginForm.jsx) + [src/components/RegisterForm.jsx](src/components/RegisterForm.jsx) wrap parent's `onSubmit` in a local `submitting` state, disable submit button + swap label to "Signing in…" / "Registering…" while in-flight. Closes the double-tap vector on a slow network. **Verification (full polish batch)**: lint 2/2 baseline warnings unchanged across every commit, `npm run test:unit` 159/159 green, `npm run build` clean (new Orbitron 500/900 WOFF2 in `dist/assets/`; total Orbitron payload across 5 weights ~32 KB woff2), targeted Playwright sweeps green (groups + comments + auth 124/124 for Chunk 5.1). **Operator post-deploy**: no manual steps — every change is additive frontend polish + read-path / cleanup work on the backend. The Phase 5 plan file `C:\Users\vinde\.claude\plans\there-are-a-few-dazzling-star.md` "Chunk 5.1 — Tier 26 P1 (15 fixes)" section has the full skip-rationale audit for the 11 deferred items.

---

## Tier 30 Phase 3 C1 — Personal stats dashboard (2026-05-31)

**Tier 30 Phase 3 C1 — Personal stats dashboard** (2026-05-31; 1 commit local). First analytics-chunk item (Chunk 3.2). New [services/StatsService.js](services/StatsService.js) aggregates the viewer's pick history into the six surfaces from the plan — `summary`, `pointsOverTime` (per-day points + running cumulative, zero-filled across the window), `winRateTrend` (per-active-day win% + 14-day MA), `perLeague` (stacked W/D/L by league), `pickTimeHeatmap` (7×24 UTC dow×hour grid of pick-submission counts), `blindSpot` (worst-team templated insight; surfaces only at ≥3 picks + ≥1 loss; no LLM), `mostDisagreedFriend` (friend with most differing picks on shared games, derived from accepted Friendship rows + cross-pick join). 5-min in-memory cache via [lib/cache.js](lib/cache.js) keyed `stats:<userId>:<window>` where window ∈ `{30d, 90d, season}` (season = rolling 1-year). Pure helpers exported for unit testing (`buildPointsOverTime`, `buildWinRateTrend`, `buildPerLeagueBreakdown`, `buildPickTimeHeatmap`, `buildBlindSpot`, `buildMostDisagreedFriend`, `utcDayKey`, `windowStartDate`). New route `GET /api/me/stats?window=<30d|90d|season>` in [routes/me.js](routes/me.js) — auth required, defaults to 30d, returns 400 on unknown window. Frontend: new lazy-loaded [src/components/StatsDashboard.jsx](src/components/StatsDashboard.jsx) (~3.30 KB gzip) consuming `recharts@3.8.1`; mounted as a new **Stats** sub-tab inside [src/components/ProfileView.jsx](src/components/ProfileView.jsx) **only when `profile.friendStatus === 'self'`** (other users' profiles don't expose the granular pick-time/blind-spot/disagreement data the dashboard surfaces — mirrors the Tier 30 Phase 3 A2 `badgeProgress` self-only convention). `React.lazy + Suspense` keeps the recharts chunk out of the eager Profile bundle. New `'charts'` chunk in [vite.config.js](vite.config.js) `manualChunks` routes recharts + d3 + victory-vendor into a single dedicated chunk (~118 KB gzip — heavier than the plan's ~15 KB estimate, but isolated to the Stats sub-tab open path). 29 new unit tests in [tests/statsService.test.js](tests/statsService.test.js) covering each pure helper (empty inputs, zero-fill, MA rolling window math, sort + dedup, BLIND_SPOT_MIN_PICKS=3 floor, disagreement counting). 5 new e2e tests in [tests/e2e/api/me.spec.js](tests/e2e/api/me.spec.js) `GET /api/me/stats` describe block (default 30d shape, 90d echo, season echo, invalid window 400, anon 401). **Verification**: lint 2/2 baseline unchanged, `npm run test:unit` 159/159 (was 130 — +29 stats cases), `npm run build` clean (new `StatsDashboard` lazy chunk 20.4 KB raw / 3.30 KB gzip; new `charts` chunk 396.72 KB raw / 118.79 KB gzip; main bundle delta negligible), full me.spec.js sweep 35/35 + profile-privacy 5/5 + pick-and-result 1/1 green. **Operator post-deploy**: no manual steps — the dashboard reads existing data via the cached aggregate. The cache self-warms on first user open per replica and self-expires on 5-min TTL; cross-replica drift is bounded by the same TTL (Tier 10.4 Redis would deduplicate later). Plan file: `C:\Users\vinde\.claude\plans\there-are-a-few-dazzling-star.md` (Phase 3 Chunk 3.2 C1 section).

---

## Tier 30 Phase 3 A5 — Weekly recap push + A6 attempted/parked (2026-05-31)

**Tier 30 Phase 3 A5 — Weekly recap push + A6 attempted/parked** (2026-05-31). **A5 Weekly recap** (commit `5a2b740`) — new [lib/jobs/sendWeeklyRecap.js](lib/jobs/sendWeeklyRecap.js) cron at `0 2 * * 1` UTC (Mondays 02:00 UTC; env override `WEEKLY_RECAP_CRON`). For each user with at least one pick scored in the trailing 7-day window, computes weekly aggregate (scored / wins / points) and fires ONE `weekly-recap` push: title "Your week on Bantryx" / body `"You went W/N this week, +P pts."` with optional flair when the user picked in exactly one league this week (`"Top X% in this league."`) and/or belongs to ≥1 group (`"Top X% in your group."` — uses the user's largest group). Cost-gated by a `Pick.count()` short-circuit before per-user work so off-season Mondays return instantly. Bulk `Pick.findAll(JOIN games)` covers all weekly picks in one query — no N+1. New push type `weekly-recap` follows dual-update rule (`PUSH_NOTIFICATION_TYPES` in [validation/schemas.js](validation/schemas.js) + `NOTIFICATION_TYPES` in [src/components/PushSettingsPanel.jsx](src/components/PushSettingsPanel.jsx)). Deep-link `/?view=profile`. Drive-by fix: stale "daily pick streak" copy in PushSettingsPanel from the original A1 updated to reflect the A1 Revision's win-streak semantics + `[5, 10, 15, 20, 30, 50]` milestone tuple. 19 new unit tests (`aggregateWeeklyStats` / `formatRecap` / `topPercent`). **A6 Pick of the Day / Coin Flip Master — attempted then parked** (commit `b74c682` ⇒ reverted by `9c278f6`). Shipped: migration adding `games.coinFlipDayKey VARCHAR(10)` + partial unique index, [lib/jobs/selectCoinFlip.js](lib/jobs/selectCoinFlip.js) cron at `30 0 * * *` UTC, `coin-flip-master` badge entry, `coinFlipWins` metric on `BadgeService.computeProgressForUser`, GameCard `🪙 Coin Flip` chip, 10 unit + 4 e2e tests. **Parked** after user surfaced two design issues: (a) one global coin-flip per UTC day (across ALL active leagues) means with PL + WC active, the more-uncertain PL match almost always wins the selection and WC matches rarely flag; (b) no look-ahead — a WC match on June 14 never gets the chip until June 14's cron run, so users browsing future fixtures see no flags. Revert was full + clean: `db:migrate:undo` ran locally first to drop the column, then `git revert b74c682` to undo every file (the migration file is deleted by the revert; DB has no orphan column, no orphan `SequelizeMeta` row). Revival recipe in [`plans/tierCoinFlipParked.md`](C:\Users\vinde.claude\plans\tierCoinFlipParked.md): replace the partial unique index with `(leagueId, coinFlipDayKey)` so each active league gets its own daily coin-flip + extend the cron to a 7-day look-ahead window (`active leagues × next 7 UTC days`). Cost stays trivial (5 leagues × 7 days × ~20 candidates per bucket per cron tick). **Verification (A5)**: lint 2/2 baseline unchanged, `npm run test:unit` 130/130 (was 111 — +19 weekly-recap cases), `npm run build` clean. **Verification (post-revert)**: lint 2/2 baseline unchanged, `npm run test:unit` 130/130 (the 19 A5 cases stay; the 10 A6 cases removed alongside the revert), DB clean (no `games.coinFlipDayKey`, no `SequelizeMeta` row for `20260531000002`), working tree clean. **Operator post-deploy (A5)**: no manual steps required — the cron registers at boot and self-fires on the next scheduled tick. To bootstrap a recap immediately, run `node -e "require('./lib/jobs/sendWeeklyRecap').run()"` inside the container.

---

## Tier 30 Phase 3 A1 Revision — Win-streak rework (2026-05-31)

**Tier 30 Phase 3 A1 Revision — Win-streak rework** (2026-05-31; 1 commit local, supersedes A1). Replaces the original calendar-day pick-streak (shipped 2026-05-30) with a per-result win streak after user feedback that lining up a month of picks in one day was a +1 increment, making the mechanic weak. New behavior: each scored pick is W (`pick.choice === game.result` for `home`/`away`), D (`game.result === 'draw'`), or L (anything else scored). W increments, D is no-op, L resets to 0. Within a same-kickoff batch (picks on games sharing `game.date`) wins are applied FIRST, then draws/losses — so `longest` captures the batch peak even when the final pick in the batch is a loss (user-wording: _"highest ever streak will be recorded including the games they won concurrently, even if one match ends before the other"_). `longest` is **monotonic** — never shrinks on a recompute. New migration [migrations/20260531000001-users-rework-streak-to-wins.js](migrations/20260531000001-users-rework-streak-to-wins.js) adds `users.{currentWinStreak, longestWinStreak, lastMilestoneFired}`, drops the 4 old daily-streak columns (`currentDailyStreak`/`longestDailyStreak`/`lastStreakDayKey`/`lastStreakFreezeMonth`), and deletes any orphan `streakmaster` badge row. [services/StreakService.js](services/StreakService.js) **fully rewritten** (~180 LOC → ~180 LOC, completely different shape): new `classify(pick, game)`, `computeStreakFromPicks(scoredPicks)` (pure — sorts by `(date, resultPriority, gameId)` with W=0, D=1, L=2; iterates), `resolveMilestone(newCurrent, prevStamp)` (dedup), `applyForUser(userId)` (async — full recompute from history via INNER JOIN against scored games). The state-machine + day/month/freeze helpers are GONE. Trigger moves from [services/PickService.js](services/PickService.js) `createPick` (hook removed) to [services/GameService.js](services/GameService.js) `{setResult, bulkSetResult, applyLiveUpdate}` via a new `fanOutStreakUpdates(picksForGame)` helper that fires once per unique `pick.userId` POST-transaction — same Tier 5.3 fire-and-forget pattern as the existing `BadgeService.evaluateBadges` calls alongside them. Streak fires on result-set AND result-clear (the recompute handles both directions naturally). Milestone thresholds rescaled: `STREAK_MILESTONES = [5, 10, 15, 20, 30, 50]` (was `[7, 14, 30, 60, 100]` for daily streaks — 60 wins-in-a-row is statistically near-zero, the new tuple matches realistic engagement curves). Streakmaster badge in [badges/catalog.js](badges/catalog.js) split into TIERS mirroring Recruiter: `streakmaster-1` (5 wins, 🌋), `streakmaster-2` (10 wins, 🌋), `streakmaster-3` (15 wins, 🌋); old single `streakmaster` slug removed. [services/BadgeService.js](services/BadgeService.js) `computeProgressForUser` swapped to read `user.longestWinStreak`; three new `tryAward` calls (5/10/15) replace the single 30-day check. [routes/me.js](routes/me.js) swaps column reads — API response shape `streak: {current, longest}` unchanged so frontend reads need no migration. [src/components/UserMenu.jsx](src/components/UserMenu.jsx) copy bumped to "N-game win streak" (label + aria-label + dropdown line) and chip brightness tiers re-aligned to 5/10/15 to match the milestone tuple. Tests fully rewritten — 35 new unit tests in [tests/streakService.test.js](tests/streakService.test.js) (classify, computeStreakFromPicks across W/W/L/W/D/L permutations, same-kickoff batch ordering, the user-wording "prev=5 then W/W/L → longest 7" scenario, pending-pick filtering, 100-shuffle determinism, gameId tiebreaker, resolveMilestone all arms); 3 new e2e tests in [tests/e2e/api/games.spec.js](tests/e2e/api/games.spec.js) `Win-streak (Tier 30 Phase 3 A1 Revision)` describe block (same-kickoff W/W/L → current 0/longest 2, monotonic longest survives result clear, pick-create on unscored game does NOT fire streak). New operator script [scripts/recompute-streaks.mjs](scripts/recompute-streaks.mjs) (idempotent ASCII-only post-deploy recompute mirroring the live JS logic; supports `--dry-run`). **Verification**: lint 2/2 baseline warnings unchanged, `npm run test:unit` 111/111 (was 95 — +16 net new streak cases after replacing 15 old daily ones), `npm run build` clean, migration roundtrip up→down→up clean, full Playwright sweeps green on touched specs (games 24/24 incl. 3 new + 5 existing crowd-gate tests; picks 27/27; me 7/7; notifications-badges 3/3 — 61/61 across the regression set). **Operator post-deploy**: after CD lands the migration, run `az containerapp exec --name scorecast-app --resource-group scorecast-prod --command "node scripts/recompute-streaks.mjs"` once to populate the new columns from each user's pick history. Idempotent. Plan file: `C:\Users\vinde\.claude\plans\there-are-a-few-dazzling-star.md` ("Phase 3 A1 Revision" section at the top).

---

## Tier 30 Phase 3 — Engagement A1–A4 (2026-05-30)

**Tier 30 Phase 3 — Engagement A1–A4** (2026-05-30 → 2026-05-31; 9 feature commits + 1 carryover on `main`; A1+A2 already pushed via parallel-session sync, A3+A4 + revision still local). Closes the first four items of the Phase 3 sticky-batch — daily streak, expanded badge catalog with Recruiter referral tier, voice-of-the-crowd indicator, and share-as-image. Plan file: `C:\Users\vinde\.claude\plans\there-are-a-few-dazzling-star.md` (Phase 3 section). **A1 Streaks** (commits `005b603` backend + `7522411` frontend) — new migration adds `users.{currentDailyStreak, longestDailyStreak, lastStreakDayKey, lastStreakFreezeMonth}`; new [services/StreakService.js](services/StreakService.js) with pure `computeNextState` decision function (UTC `YYYY-MM-DD` day keys, monthly auto-freeze covering one missed day per calendar month); fire-and-forget hook from `PickService.createPick` POST-transaction; `/api/me` surfaces `streak: {current, longest}`; `streak-milestone` push type added to `PUSH_NOTIFICATION_TYPES` + mirror in `PushSettingsPanel.NOTIFICATION_TYPES` (fires at 7/14/30/60/100 with `/?view=profile` deep-link). UserMenu trigger gains a flame chip between avatar and username (`shadow-led` at 30+); dropdown label gains "N-day streak · best M" line. 15 new unit tests cover the state machine. **A2 Referrals + expanded badge catalog** (commits `fc47366` backend foundation + `0198d6e` catalog + `eab53f4` frontend) — new migration adds `users.referralCode CHAR(8) NOT NULL UNIQUE` + `users.referredByUserId UUID → users(id) ON DELETE SET NULL` (raw SQL + IF NOT EXISTS + collision sweep, mirrors the groups.discriminator pattern); registerSchema accepts optional 8-hex `referralCode` (case-insensitive); `POST /api/register` generates code with 5x retry + stamps `referredByUserId` on successful lookup (typo-friendly — unknown codes silently ignored); `GET /api/me` surfaces `referralCode`. Catalog grew from 8 → 23 badges: Centurion / Hot Hand / Cold Plunge / Crystal Ball / Globetrotter / Roundsman / Loyalist / Margin Master / Streakmaster / Conversationalist / Friendly Five / Three's a Crowd / Recruiter I-II-III. (Coin Flip Master was originally reserved for A6 but A6 is now parked — see plans/tierCoinFlipParked.md.) New `BadgeService.computeProgressForUser` returns 16-metric snapshot driving both unlock decisions AND the frontend BadgeWall progress bars; `getProfileByUsername` surfaces `badgeProgress` ONLY on self-view (gated `viewer.id === target.id`); `evaluateBadges` chains to the referrer when the referee has scored picks (bounded at 1 level deep). New [src/components/ReferralCodePanel.jsx](src/components/ReferralCodePanel.jsx) mounted in SettingsView → Account renders the code in a `.font-led` tile with copy-code + copy-invite-link buttons. AuthContext consumes `?ref=CODE` on first mount and pre-fills RegisterForm (mirrors the existing verifyToken/resetToken consume-and-clear pattern). BadgeWall redesigned with progress bars fed from `progress[metric]` when threshold + metric are both set + badge is locked. **A3 Voice-of-the-crowd** (commit `0299448`) — new `GameService.getCrowdForGames(gameIds)` with bulk GROUP BY + 60s per-game cache via existing `lib/cache.js`; empty buckets stamped so zero-pick games don't re-query. `listGames({viewerId})` server-side gate: crowd attached only when `game.status !== 'scheduled'` OR viewer has picked. Anon viewers only see it on locked games — omitted in JSON below the gate so DevTools can't bypass the anti-bias contract. `PickService.create` + `.delete` fire `GameService.invalidateCrowd(gameId)` so the picker's own count tick lands instantly. New `CrowdMeter` component on `GameCard`. 5 new boundary tests lock the gating semantics. **A4 Share-as-image** (commits `e2af946` initial + `1172e74` dialog→direct revision + `9d1c8e6` Instagram-button drop) — `html-to-image@1.11.13` (~3 KB gzip) installed in its own Vite chunk. Initial implementation had a ShareSheet modal with format toggle; user feedback ("skip the dialogs as well") drove a refactor to direct share via `captureAndShare`: an imperative `createRoot` mount of [src/components/ShareableCard.jsx](src/components/ShareableCard.jsx) off-screen at full 1080×{1080,1920}, snapshot, then unmount. Dynamic-imports keep `react-dom/client` + `html-to-image` + ShareableCard out of the eager bundle. On mobile routes through `navigator.share({files,...})`; falls back to PNG download on desktop / share-cancel. `lib/share.js` exposes the platform shim (`isIos`/`isAndroid`/`canShareFiles`/`captureNodeToPng`/`shareFile`/`downloadBlob`/`shareImageFromNode`/`shareBlob`). Original UI shipped TWO buttons (Square + Instagram-glyph Story); user feedback ("the Instagram icon does not change anything — Instagram's picker still appears in both cases") drove dropping the Story button — the iOS `instagram-stories://share` URL scheme requires writing to `UIPasteboard` under `com.instagram.sharedSticker.backgroundImage`, a native API unreachable from a PWA. Single Square-format Share button remains; `captureAndShare(ratio)` stays parameterised so re-adding a Story button later is one JSX block away. Action row now: `[ Share | icon-left ] ........ [ "Undo" | icon-right ]` with three inline Lucide-style SVGs (ShareIcon = arrow-up-from-box, UndoIcon = counterclockwise arrow). CrowdMeter restyled from a small inline pill to a full-width row below the action row: header band ("WISDOM OF THE CROWD" + total picks count), 28 px Home/Away segmented progress bar with inline percentages baked into each segment, Home/Away label footer; segments forced to sum to exactly 100 % so rounding can't leave a sliver between them. **Carryover**: GamesCalendar "Next game day" pill (commit `292fb04`) — when the selection is on today's chip, the center slot in the 3-column header now renders a "Next game day" pill (same styling as "Back to today") that jumps to the soonest future `dayKey` in `byDay` with at least one game; omitted entirely when no future game days exist. **Verification gate (entire phase)**: lint 2/2 baseline warnings unchanged across every commit, 95/95 unit tests green (15 new streak tests), `npm run build` clean (Motion + html-to-image both in dedicated chunks), migration roundtrip up→down→up clean on both new migrations, full Playwright sweeps green on every commit (auth.spec 22/22, me.spec 30/30, picks.spec 28/28, games.spec 21/21 incl. 5 new crowd-gate tests, pick-and-result 1/1, notifications-badges 3/3, settings-view 4/4). **Plan items deferred to later commits**: A5 (post-match weekly recap cron + push), A6 (Pick of the Day + Coin Flip Master badge), C1–C4 (personal stats dashboard + ML agreement chip + team watchlist + match preview cards). **Operator post-deploy**: no manual steps required — both new users-table migrations auto-backfill (deterministic from id + collision sweep); `streakCols.lastStreakDayKey` is nullable so existing users start with `current=0` and the next pick they make starts a fresh streak; `referralCode` backfilled from first 8 hex chars of user id + uniqueness sweep on the migration.

---

## Tier 30 Phase 2 — Visual Overhaul (2026-05-30)

**Tier 30 Phase 2 — Visual Overhaul** (2026-05-30; 4 commits on `main`, not yet pushed). Lays the refined-stadium identity across the whole app via `motion/react` (~30 KB gzip in its own `motion` chunk), the new `.font-led` Orbitron numerals, a `--shadow-brand-glow-strong` four-layer bloom, a `.bg-arena-grid-bold` scoreboard backdrop, a horizontal `.mask-fade-x` ticker mask, and orchestrated motion variants in [src/lib/motionVariants.js](src/lib/motionVariants.js). Plan file: `C:\Users\vinde\.claude\plans\there-are-a-few-dazzling-star.md` (Phase 2 section). **Commit 1 `0328643`** (2.1 Foundation) — `motion@12.40` installed; [src/main.jsx](src/main.jsx) wraps the provider tree in `<LazyMotion features={domAnimation} strict>`; new [src/lib/motion.js](src/lib/motion.js) curated re-exports + [src/lib/motionVariants.js](src/lib/motionVariants.js) (8 named variants); [src/index.css](src/index.css) gains 3 new tokens + 7 new utilities; [tailwind.config.js](tailwind.config.js) gains 2 boxShadow utilities + 2 keyframes (`ticker-scroll`, `led-flicker`); [vite.config.js](vite.config.js) `manualChunks` routes motion into its own chunk; Avatar.jsx token violation fixed (text-white → inline color: '#ffffff'); Orbitron 600/700 added to the Google Fonts request in [index.html](index.html). **Commit 2 `a4dbbfa`** (2.2 Landing + Auth) — Landing hero cascade reveal (heroRevealTimeline / heroWordmark / heroRevealItem) + heavier `.text-shadow-brand-glow-strong` wordmark + heavier `shadow-brand-glow-strong` primary CTA; new league ticker strip between hero and stats (motion-driven horizontal scroll on a duplicated row with `.mask-fade-x` edges); stats grid count-ups via `useMotionValue` + `animate()`; asymmetric 3-col feature grid (cards 1, 4 tall via `md:row-span-2`; cards 2, 3 short); new [src/components/FeatureIcon.jsx](src/components/FeatureIcon.jsx) replaces the four emoji with custom inline SVGs (target/group/trophy/medal) on `border-default + bg-overlay + shadow-led` plaques; Steps numerals use `.font-led`; AuthView gains a `md:grid-cols-[1fr,1.05fr]` split — left column hosts a new AuthAmbient decorative aside (`.bg-arena-grid-bold + .bg-stadium-vignette` + LIVE chip on the `animate-led-flicker` keyframe), right column stacks LoginForm above RegisterForm. **Commit 3 `be3490d`** (2.3 Sidebar + top bar) — NavItem's static active stripe promoted to `<m.span layoutId="sidebar-active-indicator">` so the indicator slides between tabs via the `sidebarTabIndicator` spring (380/32); icons bumped h-5 → h-6; mobile drawer header band now carries `.bg-arena-grid-bold + border-b border-default`; DashboardView desktop top-bar right cluster gains a 1px vertical `bg-divider` strip as its leftmost child to anchor the action cluster. **Commit 4 `528dfa4`** (2.4 GameCard / Calendar / Leaderboard / EmptyState) — new ScoreTile sub-component renders each side's score on a `bg-overlay/60` rounded-lg plate with `.font-led + shadow-led` and an `<AnimatePresence mode="popLayout" initial={false}>` wrapping `<m.span key={String(score)}>` so the digit rotates in/out on the X axis when the score changes; "Awaiting first score" placeholder uses the same tile shape with en-dash glyphs; PayoutMatrix restyled as a 6-cell scoreboard grid (`gap-px bg-divider` against a `bg-overlay/70` cell fill; `.font-led tabular-nums` values; preserves the dual draw payouts +x/+y for home/away picks); Undo button gains `min-h-[44px] text-sm`; CommentThread wrapped in a `border-t border-default/60 mt-5 pt-5` separator; GamesCalendar today-chip day-number conditionally adopts `.font-led`, live-dot inner circle picks up `motion-safe:animate-led-flicker`, arrow SVG nudges ±2 px on hover (motion `whileHover` spring 380/22); LeaderboardCard rank-primary hierarchy lands — rank promoted to a 36×36 `rounded-xl` pill in `font-display tabular-nums text-lg` with top-3 colour-coded (#1 gold `bg-warning/40 shadow-led`, #2 silver `bg-fg-subtle/30`, #3 bronze `bg-warning/20`); Avatar bumped 28 → 32 px; EmptyState defaults to a stadium silhouette SVG when no `icon` prop passed (caller can still pass `icon={null}` to suppress). **Verification gate**: lint 2/2 baseline warnings unchanged, 80/80 unit tests green, build clean, **411/411 e2e tests pass + 1 skipped** (full Playwright suite in 3.5 min) covering every Phase 2 surface; bundle delta — motion chunk 30 KB gzip (own chunk; ~25-30 KB is heavier than the plan's 12-15 KB estimate because `domAnimation` covers variants + layoutId + AnimatePresence + gestures — necessary for the Sidebar indicator + score flip), main bundle delta 59.6 KB → 61.3 KB gzip (+1.7 KB, under the plan's < 5 KB budget). **Operator post-deploy**: no manual steps; design system change only. **Plan items deliberately deferred from Chunk 2.2-2.4**: pick-confirm burst (6 dots fan-out — adds state-coordination complexity for low ROI), submit-success ripple, password-mismatch motion SVG (existing inline error text covers the same UX), `.input-stadium` applied to auth + search inputs (existing primitive styling is functionally identical; `.input-stadium` stays in the library for Phase 4+ DM/poll surfaces), LeaderboardRow shimmer on mount + badge-unlock burst (low signal in static test data — pull forward in Phase 3 when streak/badge surfaces actually fire animations).

---

## Tier 30 Phase 1 — IA / Navigation Redesign (2026-05-30)

**Tier 30 Phase 1 — IA / Navigation Redesign** (2026-05-30; 7 commits on `main`, not yet pushed). Plan file: `C:\Users\vinde\.claude\plans\there-are-a-few-dazzling-star.md` (Phase 1 section). Sidebar grew from 6 → 7 items (adds Friends as a top-level surface); UserMenu drops a "Settings" item that opens a dedicated SettingsView; every multi-section view restructured into sub-tabs via a new shared `<SubTabs />` primitive that syncs to URL `?tab=`. Five new views + components: `src/views/SettingsView.jsx`, `src/views/FriendsView.jsx`, `src/views/GroupsView.jsx`, `src/views/LeaderboardView.jsx`, `src/components/SubTabs.jsx`, `src/components/EditProfileModal.jsx`, `src/components/CreateGroupModal.jsx`. The all-in-one `FriendsList.jsx` component was deleted; its UX split across the new FriendsView's All / Requests / Find people sub-tabs. Notification deep-links updated to target sub-tabs where it improves UX: `routes/friends.js` emits `/?view=friends&tab=requests` (sent) / `/?view=friends&tab=all` (accepted); `services/BadgeService.js` emits `/?view=profile&tab=badges`. Legacy redirect in `DataContext.consumeDeepLinks` rewrites pre-Phase-1 `view=groups` (no groupId) to `view=friends` so in-flight friend-request notifications still resolve coherently; the deleteGroup notification at `services/GroupService.js` was bumped to `/?view=groups&deleted=1` so the redirect correctly skips it. **Commit 1 `3ee46c0`** (1.1 Settings extraction) — 5 panels lifted from ProfileView into SettingsView's Account/Appearance/Notifications/Privacy sub-tabs; inline displayName+bio edit form became EditProfileModal; UserMenu adds Settings item. **Commit 2 `7878710`** (1.2 Friends tab) — FriendsView with 3 sub-tabs; sidebar entry added; routes/friends.js link bump + legacy redirect. **Commit 3 `d5c3d71`** (1.3a Profile sub-tabs) — Overview/Badges/Activity sub-tabs in ProfileView; BadgeService notification link bumped to `?tab=badges`. **Commit 4 `c711783`** (1.3b Groups sub-tabs + CreateGroupModal) — My Groups/Discover/Invites sub-tabs in new GroupsView; CreateGroupModal replaces inline form; anon defaults to Discover. **Commit 5 `fcc7248`** (1.3c My Picks sub-tabs) — Mine/Friends' segmented toggle promoted to SubTabs; status filter rail lifted above sub-tabs. **Commit 6 `d49b406`** (1.3d Leaderboards sub-tabs) — Overall/Groups/Friends sub-tabs in new LeaderboardView; Friends sub-tab is a client-side filter over the existing overall payload re-ranked locally. **Commit 7 `7797e91`** (1.3e Admin sub-tabs) — AdminPanel restructured into Leagues/Games/Users/Audit sub-tabs; Games is the default. **Test coverage added**: 5 new e2e specs covering every new surface — `settings-view.spec.js` (4 tests), `friends-view.spec.js` (5 tests), `groups-view.spec.js` (4 tests), `picks-history-tabs.spec.js` (3 tests), `leaderboard-view.spec.js` (3 tests), `admin-subtabs.spec.js` (3 tests) = 22 new tests; the existing `friend-system.spec.js` + `group-lifecycle.spec.js` + `change-(email|password)-panel.spec.js` + `admin-panel.spec.js` were updated for the new navigation. Lint 2/2 baseline warnings unchanged, npm run test:unit 80/80 green, npm run build clean (new lazy chunks: SettingsView 30KB, FriendsView 12KB, GroupsView 14KB, LeaderboardView). **Operator post-deploy**: no manual steps required — every URL convention is additive + the legacy redirect catches in-flight notifications. The user-facing change is immediately visible (sidebar item count + sub-tab navigation across every surface).

---

## Tier 30 Phase 0 — pre-launch quick wins (2026-05-30)

**Tier 30 Phase 0 — pre-launch quick wins** (2026-05-30; 7 commits on `main`, not yet pushed). Closes the highest-yield Tier 29 + Tier 26 P0 items ahead of the marketing push. Plan file: `C:\Users\vinde\.claude\plans\supertier30.md` (Phase 0 section). **Commit 1 `3b8f3e6`** — Tier 29 small UX: T29-2 football-data.org attribution in Terms §5 + lastUpdated bump (no CURRENT_TERMS_VERSION bump — additive clarification); T29-3 removes the right-column "Live leaderboard" panel from the Games tab (was a duplicate of the Leaderboard tab and narrowed the calendar); T29-4 native-popup readability fix on PicksHistory Friend + Status `<select>` (bg moved off the outer label onto the select itself so the OS-rendered dropdown inherits `bg-elevated/90`). **Commit 2 `72a0041`** — T29-5 PWA refresh button: new [src/components/RefreshButton.jsx](src/components/RefreshButton.jsx) mounted in the DashboardView top bar (between user/anon block and the bell) — calls `DataContext.revalidate()` when authed / `loadAnonDashboard()` when anon + defensively `postMessage({type:'SKIP_WAITING'})` so a waiting SW activates immediately; [src/sw.js](src/sw.js) gains a listener for that message. Closes the user-reported "installed PWA stays stale until I close + reopen" frustration (SWR cache + React state mismatch). **Commit 3 `2490e7d`** — T29-1 group discriminator: new migration [20260530000001-groups-add-discriminator.js](migrations/20260530000001-groups-add-discriminator.js) (raw SQL with IF NOT EXISTS, backfill from id, collision sweep, NOT NULL + UNIQUE INDEX), `groups.discriminator CHAR(6)` on the model + every API surface (`getGroupsForUser`, `getGroupById`, `getPendingInvites groupDiscriminator`, `discoverPublic`, `/api/search`). New [lib/groupLabel.js](lib/groupLabel.js) `formatGroupLabel(group)` (server) + [src/components/GroupNameDisplay.jsx](src/components/GroupNameDisplay.jsx) (frontend). GroupService.createGroup retries up to 5x on `SequelizeUniqueConstraintError`. All 9 internal notification call sites swapped from `${group.name}` → `${formatGroupLabel(group)}`. **Commit 4 `b1d55c3`** — backend P0 fixes: P0-1 10s AbortController timeout on football-data.org fetch; P0-2 catch `response.json()` for malformed bodies → 502; P0-3 `getProfileByUsername` reads UserScoreOverall first + narrows Game.findAll to pick gameIds + caps recentPicks at DB layer + H2H joins on shared-pick intersection; P0-7 idempotent pick create (catch unique-violation, retry up to 3x); P0-8 refresh-token rotation race fix (`SELECT ... FOR UPDATE` inside `sequelize.transaction`; `setAuthCookies` accepts optional `{transaction}`); P0-9 idempotent group join request (return existing row instead of 400; race-window guard around `.create`). **Commit 5 `fcd9313`** — P0-4 verification email observability: new migration [20260530000002-users-add-last-verification-sent-at.js](migrations/20260530000002-users-add-last-verification-sent-at.js) adds `users.lastVerificationSentAt`; `sendVerificationEmail` stamps the column; new `POST /api/me/resend-verification` (sensitiveAccountLimiter 10/hr/IP, idempotent for already-verified); `GET /me` surfaces the field; ChangeEmailPanel renders "Sent N min ago" + [Resend] when unverified; AuthContext.handleResendVerification owns the call. Register's failed-send log severity bumped warn → error. **Commit 6 `f31f5ad`** — frontend P0 fixes: P0-5 NotificationBell load + poll gated on `user?.id` so 401-loop after sibling-tab logout cleanly stops via the existing `clearSession` path; P0-6 `consumeDeepLinks(gamesList, groupsList)` toasts when game/group doesn't resolve + strips the param (anon boot passes `null` for groups so it doesn't false-toast on a valid public-group link). **Commit 7 `cd4b9e8`** — clickable member names on GroupCard (consistency follow-up the user added): member tiles + pending-request requester names open the ProfileDrawer via `useData().openProfile(username)`. Self + Tier 8.6 masked rows stay as static `<span>`s. Verified end-to-end: lint 2/2 baseline warnings unchanged, `npm run test:unit` 75/75 green, `npm run build` clean, migration roundtrip clean on both new migrations. **Not yet pushed** — operator should review the 7 commits before `git push origin main`. **Operator post-deploy**: no manual steps required — the discriminator migration auto-backfills, the lastVerificationSentAt column is nullable, and the resend route is opt-in for users.

---

## Beta → launch reset + Beta Tester badge (2026-05-28)

**Beta → launch reset + Beta Tester badge** (2026-05-28). Ahead of the marketing launch, reset every user's score to zero and granted a commemorative `beta-tester` badge (🧪) to all current users. Scores derive entirely from picks (the leaderboard reads the materialized `user_scores` tables; profiles recompute live from picks × games in [services/UserService.js](services/UserService.js) `getProfileByUsername`), so a durable, consistent zero means removing the picks — not just zeroing the materialized tables. The beta ran on the last two Premier League rounds: those PL games were bulk-deleted via the admin **Game Manager** (`GameService.cascadeDelete` reverses each pick's `user_scores` contribution + `LeaderboardService.invalidate('all')` → scores zeroed through the service layer, NOT raw SQL), and **Premier League was deactivated** (`leagues.active=false` in League Manager) so the daily `syncFixtures`/`syncLiveScores` (active-leagues-only) can't resurrect the deleted games over the off-season. The 176 residual picks turned out to be on the live 2026 World Cup fixtures (the only games left in the DB; PL had no rows) — per the clean-slate decision those were wiped too. Two new operator scripts: [scripts/grant-beta-badge.mjs](scripts/grant-beta-badge.mjs) (`--wipe-picks` deletes all picks + clears `user_scores`/`user_scores_overall` in one tx; deletes the pick-derived badges `first-pick`/`first-win`/`correct-10|25|50`/`upset-specialist` that go unearned once picks are gone; **keeps `group-founder`**; grants `beta-tester` to every user via `ON CONFLICT ("userId", slug) DO NOTHING`; `--dry-run`; ASCII-only stdout) and [scripts/notify-beta-badge.mjs](scripts/notify-beta-badge.mjs) (announces the badge to all users via `NotificationService.notify` → in-app bell + Web Push fan-out; **idempotent** — skips any user who already has the `Badge earned: Beta Tester` notification; sets `LOG_LEVEL=silent` + `DOTENV_CONFIG_QUIET=true` + `sequelize.options.logging=false` before/after a dynamic `import()` of models so the `az containerapp exec` cp1252 decoder never sees a non-ASCII byte — the dotenv `◇` banner + Sequelize `Executing` query logs were the landmines; `--dry-run`). The `beta-tester` slug is display metadata in [badges/catalog.js](badges/catalog.js) — a **manual-grant** badge, never produced by `BadgeService.evaluateBadges`; the BadgeWall renders it automatically because the catalog ships in the profile API payload. Commits `0affff1` (catalog + initial badge-only script) + `6bae70f` (`--wipe-picks` flag) + the notify-script commit. Verified on prod: leaderboard all 39 users at 0, profiles 0 pts / 0 picks, badges = `beta-tester` (+ `group-founder` where earned). **Open obligation (July 2026):** when 2026/27 PL fixtures publish (~mid/late June), reactivate PL (`active=true`), resync the new season, and train a fresh PL model (update `ml/data/raw/PL_*.csv` → `python -m scorecast_ml train --league PL` → copy to [lib/ml/models/PL_elo.json](lib/ml/models/PL_elo.json)); Elo carries over across seasons so a full rebuild (delete PL `teams` rows + re-seed) is optional. Plan file: `C:\Users\vinde\.claude\plans\for-all-the-users-replicated-flute.md` (Part 2 runbook).

---

## intl-model — World Cup predictions via international Elo + per-tournament K-mult + neutral-venue symmetrization (2026-05-28)

**intl-model — World Cup predictions via international Elo + per-tournament K-mult + neutral-venue symmetrization** (2026-05-28). Trains a separate XGBoost booster [lib/ml/models/INT_elo.json](lib/ml/models/INT_elo.json) (615 trees, val mlogloss 0.883) on the martj42 international football archive (49,215 matches × 333 teams × 1872–2026, committed at [international_match_archive/](international_match_archive/)). **FIFA-style K-multiplier tiers** — WC ×3.0, WC qualifiers + continental finals ×2.5, continental qualifiers + Nations League ×2.0, Confed Cup + Olympics ×1.5, friendlies ×1.0 — applied as BOTH the Elo K-factor weight AND the XGBoost row `sample_weight` so high-stakes matches dominate both the rating arithmetic AND the gradient. **Neutral-venue fixtures** (default for WC sync; manually settable via the new `games.neutralVenue` boolean column) get inference-time symmetrization (avg of forward + swapped predictions) guaranteeing `predict(A, B) === predict(B, A)` mirror-invariant on neutral pitches. **Single INTL meta-pool**: the existing seeded `WC` league row hosts all 200+ countries with shared Elo; future Euros/Copa wiring slots in via the same row. **Schema** (migration [20260528000002-games-add-intl-neutral-and-tier.js](migrations/20260528000002-games-add-intl-neutral-and-tier.js)): `games.neutralVenue BOOLEAN NOT NULL DEFAULT FALSE` + `games.eloKMultiplier NUMERIC(4,2) NULL` (null = treated as 1.0 at apply time). Both orthogonal to the Tier 17 snapshot matrix (`homeEloPre`/`awayEloPre`/`appliedResult`); the cascade reads them for BOTH the reverse and re-apply legs so a WC result edit (X→Y) reverses cleanly under K-mult=3. **Python pipeline**: new [ml/scorecast_ml/ingest/international.py](ml/scorecast_ml/ingest/international.py) reads `results.csv` + `former_names.csv` with date-windowed historical-name rewriting (USSR→Russia 1924-1991, Czechoslovakia variants, etc.); [ml/scorecast_ml/elo/engine.py](ml/scorecast_ml/elo/engine.py) `EloConfig` gains optional `k_multiplier_column` + `neutral_column` (default-off → PL training stays bit-identical); [ml/scorecast_ml/cli.py](ml/scorecast_ml/cli.py) `train` gets `--source international` branching, date-based train/val split (`--val-start-date 2022-01-01`), and `promoted_team_strategy='initial'` (nations don't "promote" across confederations); [ml/scorecast_ml/train/model.py](ml/scorecast_ml/train/model.py) `train()` accepts optional `sample_weight: pd.Series | None`. **JS runtime**: [lib/ml/eloMath.js](lib/ml/eloMath.js) `eloDelta(homeElo, awayElo, result, { kMultiplier = 1, neutral = false })` (default opts collapse to bit-identical PL output); [services/PredictionService.js](services/PredictionService.js) `MODEL_PATHS.WC = INT_elo.json` + `onResultUpdated` reads `game.eloKMultiplier` + `game.neutralVenue` and passes through to `eloDelta` + `rePredictFutureFixtures` symmetrizes when `game.neutralVenue === true`; [services/LeagueService.js](services/LeagueService.js) `upsertFixture` stamps `neutralVenue=true, eloKMultiplier=3.0` when `league.sourceLeagueId === 'WC'`. **JS seeder**: [seeders/20260528000003-seed-teams-from-intl-elo-history.js](seeders/20260528000003-seed-teams-from-intl-elo-history.js) (calls into `lib/ml/eloMath.js` directly to structurally eliminate one of the two parity-drift paths the PL seeder is grandfathered into). **Verification**: 75 JS unit + 36 Python + 97 e2e API/UI specs all green; PL prediction parity confirmed at the production-equivalent JS-inference layer (re-trained PL booster produces bit-identical predictions across test inputs); end-to-end cascade smoke on dev DB captured a Spain vs Argentina WC fixture → ±25.52 Elo delta (3× standard K=20 baseline), X→Y reversal returns to pre-snapshot exactly, idempotent re-save no-ops, 6 future fixtures rewritten with symmetrized probabilities. **Prod rollout**: 9 commits total — `12f8762` (feature: schema + math + ingest + training CLI + INT_elo.json + seeder + cascade + LeagueService stamp + dataset CSVs), `318b8c7` (Dockerfile fix: `COPY international_match_archive` for seeder access), `f1e4df5`/`4087e55` (`scripts/run-int-seed.mjs` ASCII wrapper for `npx sequelize-cli` under `az containerapp exec`), `daeb49c` (`scripts/inspect-wc-state.mjs`), `01667d7`/`28dfa44`/`0cc6552`/`5afae6d` (`scripts/fixup-wc-state.mjs` — handles `games.timestamps: false` gotcha, the 4-team football-data.org ↔ dataset name synonym map, subprocess-isolated cascade so pino's `fs.write(1, ...)` doesn't crash the Azure CLI), `ad43917` (intermediate stdout-buffering attempt before pivoting to subprocess), `dd4fb62` (`scripts/list-wc-team-elo.mjs`). **Operator activation sequence**: (1) deploy lands via push to main; (2) `az containerapp exec --command "node scripts/run-int-seed.mjs"` populates 333 nations under the WC league pool; (3) inspect via `node scripts/inspect-wc-state.mjs`. **The prod activation surfaced 3 issues that became permanent operator-runbook entries**: (a) 104 pre-shipped WC fixtures lacked `neutralVenue`/`eloKMultiplier` stamps because they synced before the new code (fix: `fixup-wc-state.mjs` UPDATEs them idempotently); (b) the TBD team row existed at elo=1500 from `ensureTeamExists` auto-insert during the knockout-placeholder fixture sync (fix: `fixup-wc-state.mjs` deletes it; cascade then skips TBD games via its null-elo guard, preserving the user's "no probabilities on TBD vs TBD" requirement); (c) 48 nations (every 2026 WC participant) were stuck at elo=1500/gamesPlayed=0 because the seeder's `ON CONFLICT DO NOTHING` preserved their auto-inserted rows over the seeder's computed values (fix: `fixup-wc-state.mjs` re-walks history in-process, UPDATEs `WHERE gamesPlayed=0`). **Final prod state**: 104 WC games all stamped `neutralVenue=true, kmult=3.00`; 337 teams under WC; 72/104 fixtures got INT-model probabilities, 32 TBD-containing knockouts skipped (by design); top international Elo (Spain 2091, Argentina 2039, France 2018, England 1971, Portugal 1932) matches dev. Cost added: $0/mo (in-process inference, dataset committed to git ~3.6 MB image overhead). Plan file: `C:\Users\vinde\.claude\plans\we-need-to-train-sleepy-rainbow.md`.

---

## Tier 25 — Pre-launch capacity ladder Phase 1 + Phase 2 (2026-05-28)

**Tier 25 — Pre-launch capacity ladder Phase 1 + Phase 2** (2026-05-28). Pre-launch hardening + parked-lever runbook. The plan file was rewritten earlier in the day to reflect the post-Tier-24 reality (C1 Redis rationale downgraded, C4 marked shipped) — see [tier25.md](C:\Users\vinde.claude\plans\tier25.md) and [LAUNCH_CHECKLIST.md](LAUNCH_CHECKLIST.md) for the full why + what-to-do. **Phase 1 — Tier A free wins** (commit `e532008`): **A1** Sequelize pool `{max: 20, min: 2, idle: 10_000, acquire: 30_000}` in both [config/database.js](config/database.js) (sequelize-cli) and [models/index.js](models/index.js) (runtime) — lifts the cluster-wide DB-bound concurrency ceiling from ~15 (default `max: 5` × 3 replicas) to ~60 (new `max: 20` × 3 replicas). Postgres B1ms has ~100 `max_connections` headroom. **A2** Cache-Control on static assets in [server.js](server.js): `/assets/<hashed>.*` → `public, max-age=31536000, immutable` (Vite emits hash-versioned bundles so caching forever is safe); everything else at dist root (`index.html`, `sw.js`, `registerSW.js`, `manifest.webmanifest`, PWA icons) → `no-cache` so service-worker / PWA manifest updates roll out on next page load. SPA-fallback `sendFile` also gets `no-cache`. Smoke-verified live on prod (curl headers). **A5** Container App `maxReplicas: 3 → 10` in [infra/modules/app.bicep](infra/modules/app.bicep) — 5× burst capacity at $0 idle cost (Consumption profile bills per-replica-active-time). Realistic launch shape (2-3 replicas warm during match windows): +$15-40/mo. **Phase 2 — Tier B day-1-of-marketing** (commit `d47e415`): **B1** `minReplicas: 0 → 1` in [infra/modules/app.bicep](infra/modules/app.bicep) — kills cold starts. ~$8-12/mo for one always-on 0.5 vCPU / 1 GiB replica. ~~**B2** `geoRedundantBackup: 'Disabled' → 'Enabled'` in [infra/modules/db.bicep](infra/modules/db.bicep) — attempted, reverted~~ — see the Postgres B2 gotcha invariant above. Folded into Tier 25 C3 (which recreates the server for the GP tier bump). **Operator runbook** (commit `7603e76`): new [LAUNCH_CHECKLIST.md](LAUNCH_CHECKLIST.md) at repo root with the full Bicep reapply recipe (5 params: customDomain + customDomainCertId + pgAdminPassword + vapidPublicKey + imageTag) + Step 2 A7 App Insights alert configuration walkthrough (5xx > 1%, /readyz failures, replica-cap pinned) + Phase 1 verification block + Phase 3 trigger-driven table covering A6/B-future/C1/C2/C3/C5 + deferred A3/B5 Cloudflare recipes + rollback recipes + Quick-reference table mapping every lever to its status + recurring cost. **Bicep reapply executed 2026-05-28** — 2m 10s, `provisioningState: Succeeded`, no errors. Post-apply verification: `minReplicas: 1`, `maxReplicas: 10`, readiness probe path `/healthz → /readyz` (Tier 20 Chunk 7 alignment that wasn't live until this reapply — the prior `az containerapp update --image` CD never touches probe config; a replica with a dead DB connection had been staying in rotation incorrectly), 1 replica running, /healthz 251ms first hit / ~80ms steady (cold starts dead). **Cleanup commit `dd9f689`**: reverted db.bicep B2 to 'Disabled' to match reality, folded B2 into Tier 25 C3 in tier25.md + LAUNCH_CHECKLIST.md, added the create-time-only gotcha to CLAUDE.md Critical considerations. **Net recurring cost added: ~$10/mo** (B1 only; A5 stays $0 until traffic uses extra replicas; B2 didn't apply so no $3/mo charge). **What's left from Phase 1**: A7 App Insights alerts (3 portal rules, ~15 min operator action; LAUNCH_CHECKLIST.md Step 2 walks through it). **What's NOT in scope here**: A3/B5 Cloudflare orange-cloud (deferred until bot/DDoS signal), A6 LOG_LEVEL=warn (only ship if Log Analytics quota alert fires), Tier C levers (C1 Redis / C2 Postgres B2s / C3 GP / C5 SSE — all trigger-driven with documented thresholds).

---

## Tier 24 — Materialized leaderboard scores

**Tier 24 — Materialized leaderboard scores** (pre-launch hardening; 2026-05-28). Replaces the O(picks × games) JS aggregation in [lib/users.js](lib/users.js) `buildUserSummary` + [lib/groups.js](lib/groups.js) `buildGroupLeaderboard` with two materialized tables maintained incrementally on every score-affecting write. Pulled forward from trigger-driven (post-launch when picks > 10k AND P95 > 200ms) because the dual-writer carries non-zero risk of silently incorrect rankings and pre-launch is the safe-refactor window — also eliminates the Tier 25 C1 (managed Redis, ~$16/mo) dependency since materializing at write time removes the cross-replica cache coherence problem. **Chunk 1**: new `user_scores (userId, leagueId, seasonId, points, picksScored, picksWon, updatedAt)` table keyed on the compound PK, with a partial index `(leagueId, seasonId, points DESC, userId) WHERE points > 0` covering the top-N read; new `user_scores_overall (userId, points, picksScored, picksWon, updatedAt)` table for the unfiltered overall (single-PK lookup, no synthetic-UUID hack); new `picks.appliedResult VARCHAR(10) NULL` + `picks.appliedPoints INTEGER NOT NULL DEFAULT 0` idempotency sentinels mirroring Tier 17's `games.{homeEloPre, awayEloPre, appliedResult}` pattern. Migration `20260528000001-tier24-create-user-scores.js` uses raw SQL with `IF NOT EXISTS` per the CLAUDE.md invariant. New models `models/UserScore.js` + `models/UserScoreOverall.js` + association wiring in [models/index.js](models/index.js) (with `onDelete: 'CASCADE'`). New `scripts/backfill-user-scores.mjs` — idempotent operator script. New `tests/userScore.test.js` (18 pure-function tests covering computePoints + deriveCounterDeltas + round-trip invariants). **Chunk 2**: new `services/UserScoreService.js` with `applyDelta(transaction, opts)` (INSERT-ON-CONFLICT DO UPDATE; atomic Postgres increment) + `applyPickTransition(transaction, {pick, game})` (8-arm idempotency matrix: null→home/away/draw, home→away/draw/null, idempotent re-save no-op) + `reversePick(transaction, {pick, game})` (used by cascade paths). 7 write hooks wired: `PickService.{createPick, deletePick}`, `GameService.{setResult, bulkSetResult, applyLiveUpdate, cascadeDelete (used by deleteGame + bulkDelete)}`. All hooks run INSIDE the existing service-layer transaction so the materialized write rolls back atomically with the originating mutation. `LeaderboardService.assertParity({userId})` parity-log helper compares materialized totals against `buildUserSummary` after every write hook fires; gated on `PARITY_LOG_ENABLED=1` so default-off in prod. `UserService.cascadeDelete` extended with explicit `UserScore.destroy` + `UserScoreOverall.destroy` mirroring the existing token-table cleanup (defense-in-depth against any future sync()-vs-migration FK ordering surprise). Test helpers `clearGameResults` + `clearPicksAndBadges` updated: `clearGameResults` now routes through `GameService.setResult(null)` per-game so the dual-writer reverse fires through the actual production code path; `clearPicksAndBadges` drops user_scores rows alongside Pick.destroy. **Verification gate (Layer 1-4)**: Layer 1 — existing leaderboard + picks + games + leaderboard-scoring specs all 50/50 green; Layer 2 — new [tests/e2e/api/tier24-user-scores.spec.js](tests/e2e/api/tier24-user-scores.spec.js) (22 tests covering matrix arms 1-8, pick lifecycle, bulk paths, cascade, round-trips, concurrency, league/season scoping, global parity) all 22/22 green; Layer 3 — new `scripts/exercise-user-scores.mjs` deterministic exercise (8 transitions × 2 users × 1 game = 0 drift); Layer 4 — manual UI smoke covered by existing pick-and-result.spec.js (UI flow including LeaderboardCard render) — green. Full e2e sweep: 401/406 pass (3 mobile-screenshot drifts are pre-existing layout shift; 2 admin/comment flakes pass in isolation). **Chunk 3**: `services/LeaderboardService.js` `getOverall` + `getForGroup` rewritten to read from the materialized tables. Unfiltered overall = single `UserScoreOverall.findAll() ORDER BY points DESC`; filtered = `UserScore.findAll({where: {leagueId, seasonId}})`; group = `UserScore` (or overall) `WHERE userId IN (group member set)`. Users with zero in-scope picks LEFT-JOINed in at `points: 0` to preserve the post-Tier-8.6 "no member drop" invariant. Legacy `buildUserSummary` + `buildGroupLeaderboard` kept alive behind `TIER24_LEGACY_LEADERBOARD=1` env var for one-cycle rollback. **Chunk 4**: `routes/leaderboard.js` returns `{overall: top50, overallMeta: {rows, total, viewerRow, offset, limit}, group, groupMeta}`. New `LeaderboardService.getOverallSlimForViewer({overallOffset, overallLimit, leagueId, seasonId}, viewer)` applies the masking-then-slice (so ranks stay unmasked-order). `validation/schemas.js` `leaderboardQuerySchema` accepts `overallOffset` (max 10000) + `overallLimit` (max 500). `DataContext.refreshLeaderboard` updated to carry `overallMeta` through to consumers; LeaderboardCard's existing Tier 18 Chunk 3 compact mode continues to work unchanged because `data.overall` is still an array (just shorter). **Chunk 5 (cache drop)**: deferred indefinitely. Reads are sub-ms on the materialized path, but keeping the 30s cache as a thin per-replica buffer absorbs concurrent identical requests at near-zero cost. **Performance**: pre-Tier-24 leaderboard rebuild was O(picks × games × users) in-memory JS; at launch volumes (10k users × 50 picks × 2k games = ~1B ops) the existing 1 GiB Container App replica would OOM during a rebuild. Post-Tier-24: top-50 read is a single B-tree index scan against the partial index `WHERE points > 0`. **Cost**: $0/mo recurring; eliminates Tier 25 C1's $16/mo by removing the cross-replica cache-coherence problem at the source. **Shipped end-to-end 2026-05-28** via commits `23789bb` (full feature: schema + dual-writer + read switch + slim payload + doc sweep) + `0ee4d64` (chore: `COPY scripts ./scripts` in Dockerfile runtime stage so `scripts/backfill-user-scores.mjs` is reachable via `az containerapp exec`; same gotcha previously hid Tier 17's operator scripts — fixing it unblocks `query-teams.mjs`, `find-game.mjs`, `repair-test-game-elo.mjs`, `backfill-probabilities.mjs` too). Both CD cycles green; `bantryx.com/healthz` 200; `/api/leaderboard` returns the new slim `{overall, overallMeta, group, groupMeta}` shape. **Operator backfill against prod** is a one-line `az containerapp exec --name scorecast-app --resource-group scorecast-prod --command "node scripts/backfill-user-scores.mjs"` after the deploy lands — required to populate `user_scores` rows for picks scored BEFORE the migration (the dual-writer only fires on future transitions; pre-existing scored picks with `appliedResult=NULL` remain invisible to the materialized totals until something touches their game's result OR the backfill stamps them). Idempotent — re-running produces identical state.

---

## Tier 22 — Park 2FA + pre-launch security hardening

**Tier 22 — Park 2FA + pre-launch security hardening** (3 commits + 1 cleanup; 2026-05-27). Two parallel threads bundled into one PR ahead of the marketing launch. **Phase 1 — Park 2FA** (commit `b2bd286`): removed the 4 route handlers (POST `/auth/2fa/verify` + `/me/2fa/{setup,confirm,disable}`) + the login `challenge: true` branch + 3 zod schemas + `TwoFactorSetup.jsx` + `TwoFactorChallenge.jsx` + the AuthContext + AuthView + ProfileView wiring. Kept on purpose so revival is a `git revert` away: `users.{totpSecret, totpEnabledAt, totpRecoveryCodes}` columns, every migration, `lib/auth.js CHALLENGE_COOKIE`/`CHALLENGE_TTL_MS`, the `twoFactorEnabled` field on GET /api/me, and the speakeasy/qrcode npm deps. Marker comments in routes/auth.js + routes/me.js carry the revival recipe. The 20260514000001-disable-all-2fa.js migration already cleared every user's totp columns in May 2026, so the removal lands on a clean data slate — nobody loses access. 4 new regression e2e tests assert the 4 routes return 404. **Phase 2 — Security patches** (commits `362a3a6` + `545688e` cleanup + `4c0c234`): **H1** new rate limiters (sensitiveAccountLimiter 10/hr/IP on `/me/password` + `/me/email`; lightWriteLimiter 60/min/IP on `/me/{push-preferences,onboarding-completed,accept-terms}` + PUT `/me` + `/notifications/{:id/read,read-all}`; inviteLimiter 5/min/IP on group invite). **H2** group invite rate limit + per-group pending-invite cap (in plan; rate limit shipped, cap follow-up). **H3** comment-edit/remove IDOR fix — `assertStillMember` re-check in CommentService for group-scoped comments (admin override on delete preserved). **H4** Web Push SSRF — pushSubscribeSchema endpoint refine() rejects anything not on FCM/Apple/Mozilla/Edge allowlist or non-HTTPS; PushService.sendToSubscription defensive private/loopback-IP block (127._, 10._, 172.16-31._, 192.168._, 169.254._ incl. Azure IMDS, ::1, fc/fe80). **M4** MAX_GROUP_MEMBERS=500 (env-overridable, clamps [10, 5000]) enforced in 4 add-member paths. **Drive-by fix** to a pre-existing pushPreferencesSchema regression (Zod 4's `z.record(enum, value)` requires all enum keys, breaking partial-update merge contract) — switched to `z.record(z.string(), z.boolean())` + refine that gates against PUSH_NOTIFICATION_TYPE_SET. **C3+H5** npm audit fix for `js-cookie` (HIGH, prototype-hijack) + `qs` (MODERATE, DoS). **H6** `overrides.uuid: ^11.1.1` in package.json (resolves the moderate buffer-bounds CVE without a semver-major sequelize bump). **M3** explicit HSTS in helmet with `preload` + 2-year max-age (eligible for HSTS preload list after 30 days of prod traffic). **M2** CORS non-prod fallback locked to localhost-only (was `true`, which would silently accept any origin in a misconfigured staging env). **M1** dropped `uptime` from `/healthz` body (info disclosure: container restart timing leaks deploy patterns). **L4** extended Permissions-Policy (`camera, microphone, geolocation, payment, usb, fullscreen, accelerometer, gyroscope, magnetometer, interest-cohort` all denied). 5 new e2e boundary tests (3 push-SSRF, 2 comment-IDOR-after-leave). Group cap test left out — cap=500 makes direct e2e impractical without seeding 500 fake users; the env-overridable `MAX_GROUP_MEMBERS` knob exists for future staging tests. Verified: lint clean (2 pre-existing warnings unchanged), 42/42 unit tests green, `npm audit --omit=dev --audit-level=high` reports zero vulnerabilities, 189/189 API-spec tests green across health/auth/me/picks/groups/comments/push, 5 of 6 UI-spec failures in full e2e sweep traced to a pre-existing seed-date drift on main (today=2026-05-28, seed games at 5/29-5/31 not on default calendar chip) — NOT caused by Tier 22. **Defense-in-depth recommendations for launch** (operational, not code-shipped): Cloudflare WAF + Bot Fight Mode + edge rate limits on /api/login, Sentry alerts on /api/auth/_ 5xx + 401 spikes, Postgres connection ceiling check, backup restore drill, secrets rotation drill, security@bantryx.com or .well-known/security.txt, audit-log weekly digest. **Accepted-risk items**: Postgres firewall AllowAllAzureServices (cost-gated for Tier 10.4), no CAPTCHA on register (registerLimiter + Resend quotas cover it), no file upload surface today. Full plan at `C:\Users\vinde\.claude\plans\tier22.md`. Commits: `b2bd286` (Phase 1), `362a3a6` (Phase 2 H1-H4/M4), `545688e` (dead-import cleanup), `4c0c234` (Phase 3 deps + headers). **Shipped end-to-end 2026-05-28** via PR #19 merge-commit `f677b0f` (after the PR #18 squash-merge `c2853f2` unbroke the CI baseline + 6 e2e UI flakes). Post-deploy verification on https://bantryx.com confirmed: `/healthz` returns `{"ok":true}` (no uptime leak); `Strict-Transport-Security: max-age=63072000; includeSubDomains; preload`; `Permissions-Policy` carries all 10 directives (camera/microphone/geolocation/payment/usb/fullscreen/accelerometer/gyroscope/magnetometer/interest-cohort); `/api/auth/2fa/verify` returns the `/api/*` sentinel 404 (route handler deleted). **Operational defense-in-depth from the plan** (Cloudflare WAF + Bot Fight Mode + edge rate limits + scanner-path blocklist; HSTS preload list submission after 30 days; Sentry alert routing; Postgres restore drill; secrets rotation runbook; `security@bantryx.com` / `.well-known/security.txt`; audit-log weekly digest) is **parked in `C:\Users\vinde\.claude\plans\ROADMAP.md` "Parked items — From the Tier 22 pre-launch security plan"** — none code-level, all can be picked up before/during the marketing push. The Cloudflare batch has an orange-cloud prerequisite (current DNS is grey-cloud / DNS-only) which means a `trust proxy` audit before flipping.

---

## Tier 20 — Polish, hardening, Tier 10 fold-in

**Tier 20 — Polish, hardening, Tier 10 fold-in** (7 chunks; 2026-05-26). Polish-and-hardening tier with no new infra. **Chunk 1 — Legal + age gate + terms v2 bump**: dropped the `$50` liability floor from Terms §7 (free tier always pays $0, so the cap collapses to $0); added a 13+ age line to Acceptable Use (§3); literal-validated `confirmedAge: true` field added to registerSchema + a second required checkbox in RegisterForm. `CURRENT_TERMS_VERSION` bumped to 2 in both [validation/schemas.js](validation/schemas.js) AND [src/lib/terms.js](src/lib/terms.js) — existing users get the blocking modal once on next visit. **Chunk 2 — Profanity filter**: added `obscenity` (~17KB, MIT) with English dataset + recommended transformers (l33t / repeated-char / zero-width evasion handling) + Scunthorpe-style whitelist. Shared `noProfanity` zod refine in [validation/schemas.js](validation/schemas.js) applied to 6 surfaces: username, displayName, bio, comment body, group name, join-request message. Failed validation returns 400 with `Please remove inappropriate language`. Three boundary tests in comments.spec.js / me.spec.js / groups.spec.js lock the wiring. **Chunk 3 — Search → calendar deep-link**: SearchBar's `onSelectGame` in DashboardView now calls `navigateToDeepLink('/?gameId=<id>')` which reuses the Tier 18 Chunk 6a deep-link consumer to translate to a synthetic `?date=YYYY-MM-DD` and pre-shift GamesCalendar's `windowIndex` so the chip is visible on first paint. **Chunk 4 — GamesCalendar mobile polish**: header replaced with a 3-column grid (heading left, Back-to-today pill center when present, count right-aligned); `fullDayLabel` truncates at 360px with reduced tracking; Today chip's label switches to lowercase `Today` at `<sm:` (narrower than the uppercase 3-letter weekday labels) so it fits the ~40px chip width without the truncate ellipsis kicking in. **Chunk 5 — Logo restyle (athletic motif + center)**: B path in [public/logo.svg](public/logo.svg) shifted -25 in x (was 25px right of canvas center 256); added a horizontal cyan accent stripe at y=263 (the B's crossbar pinch point), 352px wide centered, ~85% peak opacity fading to transparent at the edges — reads as a "pitch line" at 192px+ icon sizes, vanishes at 32-64px favicon sizes. Six PNG/ICO variants regenerated via [scripts/generate-pwa-assets.mjs](scripts/generate-pwa-assets.mjs). **Chunk 6 — SEO + OG + favicon**: full Open Graph block + Twitter Card (summary_large_image) + canonical link + inline JSON-LD `WebApplication` structured data in [index.html](index.html). New 1200×630 OG image emitted from a new [public/og-template.svg](public/og-template.svg) landscape canvas (B + BANTRYX wordmark + tagline) by the extended PWA-assets script. JSON-LD body is intentionally single-line so its SHA-256 hash stays byte-stable; [server.js](server.js) `cspDirectives.scriptSrc` whitelists `'sha256-GhzleH2mfEY14NZF8AZ+UWxx4YN/y6+t46pWTLHVEUo='` to permit the inline block. **Don't reformat the JSON-LD** — any whitespace change requires re-computing the hash (one-liner documented in server.js). **Chunk 7 — Production hygiene (Tier 10.1 + 10.5 fold-in)**: `/readyz` route added to [routes/health.js](routes/health.js) — pings DB via `SELECT 1`, returns 503 on failure. `/healthz` stays liveness-only (no DB) so transient outages don't restart containers. [infra/modules/app.bicep](infra/modules/app.bicep) readiness probe now points at `/readyz`. SIGTERM + SIGINT handlers in [server.js](server.js) wrap the existing `app.listen()` return value with a 25s drain race (5s buffer under ACA's 30s `terminationGracePeriodSeconds`), then scheduler.stop() → sequelize.close() → exit(0). Tier 10.2 (Sentry server-side) + 10.3 (Prometheus) NOT folded in — kept Tier 20 focused. Tier 7 (SSE realtime + email/digests) explicitly NOT folded in — SSE needs Redis to pay off at multi-replica, email/digest is its own tier-sized scope. **Doc sweep**: this entry + new `/readyz` vs `/healthz` invariant in the Critical considerations block. ARCHITECTURE.md gains §8.29 (Tier 20 deep-dive) + updated Tier 10 status. ROADMAP.md strikes 10.1 + 10.5 from the open list. Verified: lint clean (2 pre-existing warnings unchanged), 42/42 unit tests green, build clean, auth + comments + me + groups specs all green (129 + 24 = 153 e2e tests). Commits `98a04c8` (1), `c98b48f` (2), `153e817` (3), `d1596ff` (4), `beabe56` (5), `9805631` (6), `7386936` (7). **Post-launch follow-ups** (commit `3fbb240`): (a) Chunk 3 search-tap was a no-op when already on the Games tab — `navigateToDeepLink` rewrote the URL but `pushState`/`replaceState` don't fire `popstate`, and GamesCalendar's `useState` initializer only runs on mount, so the mounted calendar stayed stale. Fixed by dispatching a `scorecast:url-changed` `CustomEvent` from `consumeDeepLinks` after the `replaceState`; GamesCalendar adds a `useEffect` listener that re-reads `?date=` and snaps `selectedKey` + `windowIndex`. Generic event name on purpose — future deep-link targets that need to react in-place can reuse the same wakeup. (b) Today-chip overflow returned (lowercase `Today` still clipped to `Toda…` on tight 360px combos); dropped the word entirely on `<sm:` and rely on the chip's accent border + cyan day-number for the "this is today" cue. `TODAY` label still renders at `sm:` and above. (c) `fullDayLabel` for non-today/tomorrow no longer prefixes the weekday (the chip strip directly below already shows it) — now just `May 27` instead of `Wednesday, May 27`. Also confirmed CD failure mode + recipe: ACA migrate-job MI principalId rotation orphans the `AcrPull` role assignment — `az role assignment create --assignee <current principalId> --role AcrPull --scope <acr id>` resolves, no Bicep reapply needed; saved as memory `reference_aca_migrate_job_acr_pull.md`.

---

## Tier 19 Chunk 5 — kickoff-time pick scoring lock (2026-05-26)

**Tier 19 Chunk 5 — kickoff-time pick scoring lock** (2026-05-26). All users picking the same outcome on the same game now score identically regardless of when in the week they picked. Migration [20260527000002](migrations/20260527000002-games-add-pick-probabilities-locked-at.js) adds `games.pickProbabilitiesLockedAt TIMESTAMPTZ NULL` + a partial index on `(status, date) WHERE pickProbabilitiesLockedAt IS NULL` so the hot cron query stays cheap on a growing games table. Two writers fire on transition out of `status='scheduled'` (defense in depth): new [lib/jobs/lockPickProbabilities.js](lib/jobs/lockPickProbabilities.js) cron at 1-min cadence (cost-gated by a `Game.count` short-circuit, so off-season ticks are near-free) AND an in-line bulk-UPDATE block in [services/GameService.js](services/GameService.js) `applyLiveUpdate` inside the same `FOR UPDATE` transaction as the status flip (atomic — never a partial state where status changed but picks aren't locked). Detection variable `transitionedOutOfScheduled` captured BEFORE the status assignment, same pattern as the existing `transitionedToFinished` — fires on ANY transition away from scheduled, so the rare scheduled → finished direct jump (upstream first observation arrives after match is done) also locks. [services/PredictionService.js](services/PredictionService.js) `rePredictFutureFixtures` gets a paranoid `pickProbabilitiesLockedAt: null` guard on top of the existing `status='scheduled'` filter so the cascade can never overwrite a locked game even if status semantics later change. [src/components/GameCard.jsx](src/components/GameCard.jsx) `PayoutMatrix` gains a one-line "Payout locks in at kickoff." tooltip below the payout grid. PickService is INTENTIONALLY unchanged — the pick-time snapshot write still happens (placeholder for any "what would this pick score right now" UI preview), the lock just overwrites it at kickoff. `'odds-shifted'` notification stays meaningful pre-kickoff (probability moves still change final payouts pre-lock); post-lock it becomes structurally impossible because the cascade is gated. New `LOCK_PICK_PROBABILITIES_CRON` env knob documented in [.env.example](.env.example), default `'* * * * *'`. Three e2e tests in [tests/e2e/api/picks.spec.js](tests/e2e/api/picks.spec.js) `lockPickProbabilities` describe block lock the invariants: identical-snapshots-after-lock (two picks at different odds end up with the same snapshot), idempotency (second cron run is a `skipped: 'no-relevant-games'` no-op), no-op on already-locked (manually-stamped game's existing snapshots are preserved even if probabilities later move). New helper `updateGameFields(gameId, fields)` in [tests/e2e/helpers/api.js](tests/e2e/helpers/api.js) lets specs stage states the public API rejects (date in past, locked-at populated, etc.). Drive-by fix on a separate pre-existing bug discovered during verification: [tests/e2e/api/picks.spec.js:251](tests/e2e/api/picks.spec.js#L251) `friend picked, game scored → points populated` test was calling `setGameResult(GAMES.lions.id, 'home')` with the gameId where the helper signature expects an admin client as the first arg — broken since commit `fde76b9` (Tier 18 Chunk 4), failing silently with `authed.post is not a function`. Now logs in as admin and passes the client through correctly. **Tradeoff doc**: the "pick early at long odds for value" gameplay loop is gone. A pick at 0.30 home odds → win that used to score +70 now scores whatever the model shows at kickoff (likely 0.35-0.45 → +55-65 on stable leagues). Loss is real but bounded; fairness gain is concrete and observable (same-team picks pay the same regardless of pick time). User made the call; CLAUDE invariant documents the don't-reintroduce-pick-time-lock guardrail. Verified: lint clean (2 pre-existing warnings), `npm run test:unit` 42/42 green, `npm run build` clean, migration roundtrip (up → undo → up) clean, picks.spec.js 27/27 green (24 existing + 3 new) in 13.3 s. Commit `<pending>`.

---

## Notification bell click-through + `odds-shifted` link fix

**Notification bell click-through + `odds-shifted` link fix** (Tier 19 follow-up, 2026-05-26). Audit revealed that every server-side `NotificationService.notify(...)` call site populates the 5th-positional `link` field (Tier 18 Chunk 6a), but the in-app [src/components/NotificationBell.jsx](src/components/NotificationBell.jsx) click handler only called `markRead(n.id)` — `n.link` was completely ignored in-app. Deep-link navigation only fired via Web Push clicks (the SW's `notificationclick` handler at [src/sw.js:147-150](src/sw.js#L147-L150)) or direct URLs with `?view=`/`?gameId=`/`?groupId=` (boot consumer at [src/contexts/DataContext.jsx:273](src/contexts/DataContext.jsx#L273)). Inside the bell — i.e. the most common interaction surface for users without push subscribed — the populated `link` field was dead. Three-file fix: (1) new `navigateToDeepLink(link)` callback on `DataContext` exported via the context value — wraps a `history.pushState` of the URL with a re-run of the existing memoized `consumeDeepLinks(games)`, so the in-app behavior is bit-identical to a cold load from a push click; bails gracefully on malformed input. (2) [src/components/NotificationBell.jsx](src/components/NotificationBell.jsx) row click handler now does three things in order: marks-read if unread, navigates via `navigateToDeepLink(n.link)` if present, closes the popover (`setOpen(false)`) — the popover-close is the natural UX for any navigation action, even if it broke a pre-existing test that depended on the popover staying open. (3) [services/GameService.js:110](services/GameService.js#L110) — `odds-shifted` notification's `link` was emitting `/games/${game.id}` (a path-form not present in the SPA router) instead of `/?gameId=${game.id}` (the documented convention). Neither consumer handled it: the SW called `clients.openWindow('/games/...')` which landed on the home page; the boot consumer reads query params only; the new bell consumer parses `new URL(link, origin)` which succeeds but `consumeDeepLinks` sees no `?gameId` and no-ops. Fixed to match the convention. Doc sweep: [CLAUDE.md](CLAUDE.md) — extended the existing "Notification deep-link convention" invariant from one consumer to three (boot / SW click / in-app bell), called out `navigateToDeepLink` as the only sanctioned bell-click target, and added the `odds-shifted` regression note. [ARCHITECTURE.md](ARCHITECTURE.md) §6.2 (consumer table extended with the in-app bell consumer + `navigateToDeepLink` description), §7.4 (the stale "Reserved for deep-linking; not yet rendered" note on the `notifications.link` column is now corrected), §8.6 (Notification subsystem extended), §8.26 (Tier 18 Chunk 6a section gains the bell-click sub-bullet). Test impact: [tests/e2e/notifications-badges.spec.js](tests/e2e/notifications-badges.spec.js) gains a new boundary test asserting badge → Profile tab + pick-scored → Games tab navigation + consumed-params are stripped; also fixed two pre-existing tests in the same file that had bit-rotted since 2026-05-26's GamesCalendar replacement (helper `pickLionsAndScore` now navigates to the lions game's date via `?date=` URL param; the Radix Portal scope bug in the unread-counter test got swapped from `tray.locator('..')` to page-scope; the mark-all-read test got a re-open-bell step because the row click now closes the popover). Verified: lint clean (2 pre-existing warnings unchanged), `npm run test:unit` 42/42 green, `npm run build` clean, `npx playwright test notifications-badges.spec.js` 3/3 green (22 s). Commit `<pending>`.

---

## Cron cost-gate on `syncLiveScores` (2026-05-26)

**Cron cost-gate on `syncLiveScores`** (2026-05-26). After noticing daily Azure Container Apps Consumption costs had ramped from ~$0.10/day pre-2026-05-21 to ~$0.77/day (visible in the Azure billing chart), traced the cause to: (a) PWA Chunk 6's `sendKickoffReminders` + the new `reconcileInProgressGames` cron landing 2026-05-19/20, and (b) Tier 18 Chunk 2's 30-s `syncLiveScores` tightening on 2026-05-23. Container Apps Consumption bills per vCPU-second of active work, not just per-request — every 30-s `syncLiveScores` tick called football-data.org + parsed the response + ran the reconcile pass regardless of whether any local game was live or imminent, so during PL off-season (mid-May → mid-August) and overnight on match days that was ~2880 wasted upstream calls/day + the CPU to handle them. Fix: [lib/jobs/syncLiveScores.js](lib/jobs/syncLiveScores.js) now runs a cheap `Game.count` early in `run()` against `{leagueId IN <active>, [(status='in-progress') OR (status='scheduled' AND date IN [now − 4h, now + 2h])]}`. When the count is 0, the tick returns `{skipped: true, reason: 'no-relevant-games'}` BEFORE the outbound API call. 4h lookback covers any kickoff that passed while the app was scaled to zero (longest realistic match ≈ 165 min: 90 + HT + injury + ET + pens); 2h lookahead catches the SCHEDULED → IN_PLAY transition the moment upstream flips. `reconcileInProgressGames` (early-returns when no local in-progress games) and `sendKickoffReminders` (early-returns when no scheduled-within-15-30-min games) already had equivalent gates — confirmed both during the change, no work needed there. Expected impact: daily Container Apps cost drops back toward the pre-2026-05-21 baseline during off-season + overnight; match-window cost unchanged. Two-file change: [lib/jobs/syncLiveScores.js](lib/jobs/syncLiveScores.js) + new CLAUDE.md invariant documenting the rationale + don't-remove/don't-shrink guardrails. Verified: lint clean (2 pre-existing warnings), 42/42 unit tests green. Commit `5e7ee75`. **Tier 18 Chunk 6 — Notification deep-links + error toast cleanup + legal pages + terms acceptance** (2026-05-26). Four bundled threads. **6a deep-links**: every `NotificationService.notify(...)` call site now populates the 5th positional `link` arg with a deep-link URL. Convention: `pick-scored`/`odds-shifted`/`kickoff-reminder` → `/?gameId=<id>`; `invite`/`group-join`/`group-comment` → `/?view=groups&groupId=<id>`; `badge` → `/?view=profile`; `friend-request` → `/?view=groups`; `deleteGroup` (group is gone) → `/?view=groups`. New `DataContext.consumeDeepLinks(gamesList)` runs ONCE between data-load and `bootDone`, recognizes `?view=` / `?gameId=` / `?groupId=`, writes synthetic `?date=YYYY-MM-DD` for the gameId path (so GamesCalendar reads it on first mount), strips consumed params via `history.replaceState`. The deep-link consumer order matters — moving it AFTER bootDone races GamesCalendar's URL-reading useState initializer. **6b errors**: AuthView.handleLogin wraps authLogin in try/catch (closes documented Tier 5.5b race that clobbered "Invalid credentials" with the generic toast). useRequest marks every 4xx with `err.wasHandled = true`; clientErrorReporter short-circuits on the flag — skips both DOM event AND server POST; NotificationContext has defense-in-depth listener check. New `FRIENDLY_ERROR_CODES` map in useRequest wraps `football_api_rate_limit` / `rate_limited` into one-line user copy. **6c legal pages**: new `src/components/legal/` — `LegalLayout` + `Terms` / `Privacy` / `Copyright` / `CookiePolicy`. Plain-English copy grounded in app data flows but DELIBERATELY trimmed (no cookie-name tables, no exact retention windows, no named sub-processors, no specific crypto) to minimize attack-surface disclosure while satisfying T&T Data Protection Act 2011 disclosure requirements. App.jsx pathname-short-circuits on `/terms` / `/privacy` / `/copyright` / `/cookies` BEFORE any boot/auth logic — anon + authed users see same content. New `Footer.jsx` mounted on Landing + DashboardView with `© <year> Bantryx · Trinidad & Tobago · [Terms] [Privacy] [Copyright] [Cookies]`. **6c terms acceptance**: migration `20260526000002` adds `users.{termsAcceptedAt TIMESTAMPTZ, termsAcceptedVersion INT}` (both nullable). `CURRENT_TERMS_VERSION = 1` constant in both [validation/schemas.js](validation/schemas.js) (server) and [src/lib/terms.js](src/lib/terms.js) (client) — must stay in sync; bumping re-prompts every user with older version. `registerSchema` requires literal `acceptedTerms: true` + literal version. RegisterForm gates submit on required checkbox with inline `/terms` + `/privacy` links opening in new tab. `routes/auth.js POST /api/register` stamps both columns on User.create so new users never see modal. New `POST /api/me/accept-terms` validates version (rejects stale tabs with 400), stamps both columns, returns new values. New `<TermsAcceptanceModal />` (Radix Dialog) BLOCKS dismissal via Escape / overlay / interact — only actions are "I accept" (POSTs) or "Sign out" (performLogout). Mounted in App.jsx when `user && !browseAsGuest && needsTermsAcceptance(user)`. OnboardingTour suppressed while terms gate is open. Seed users pre-accepted in `fixtures/seed.js`; `registerViaUI` helper ticks `#register-accept-terms`; 5 API-level `/api/register` calls in `auth.spec.js` + `admin.spec.js` updated to send the new fields. Verified: lint clean (2 pre-existing warnings), build clean, 42/42 unit tests green, migration roundtrip (up → undo → up) clean. Commit `9b61627`. **Tier 18 Chunk 5 — Group running comments** (2026-05-26). Generalizes the comment subsystem from per-game to scope-agnostic so groups can carry their own running threads. Migration `20260526000001` flips `comments.gameId` to NULLABLE, adds `comments.groupId UUID NULLABLE → groups(id) ON DELETE CASCADE`, adds partial index `comments_group_idx (groupId) WHERE groupId IS NOT NULL`, adds DB-level CHECK `comments_one_scope_chk ((gameId IS NOT NULL)::int + (groupId IS NOT NULL)::int = 1)`. `CommentService` refactored: `listForGame(gameId, viewerId)` → `list({gameId, groupId}, viewerId)`; `create({gameId, userId, body})` → `create({gameId, groupId, userId, body})`. Both call `assertSingleScope({gameId, groupId})` first so programmer errors surface as recognizable 400s instead of Postgres CHECK violations. New `fanOutGroupComment({comment, author, group})` fire-and-forget after every group create — notifies every OTHER group member with `'group-comment'` type, title `"<author> commented in <group name>"`, body capped at 160 chars, link `/?view=groups&groupId=<id>`. Wrapped in try/catch so notification outage can't break comment create (Tier 5.3 invariant). New routes: `GET /api/groups/:id/comments` (`optionalAuth` + `publicReadLimiter`; anon-readable for public groups; **404 for non-members of private groups** to avoid existence leak — mirrors `GroupService.getVisible`); `POST /api/groups/:id/comments` (auth + CSRF + `commentLimiter`; membership enforced in service layer with 403 even on public groups — write is member-only by design). `GroupService.cascadeDelete` explicitly destroys group comments + reactions inside the transaction (defensive against `sync({alter:false})` bootstrap paths where FK might have landed as NO ACTION). `CommentThread` generalized to `{scope, scopeId}` props with backwards-compat `gameId` shim (existing GameCard callers untouched). `GroupCard` mounts `<CommentThread scope="group" scopeId={group.id} />` for members + owner. `PUSH_NOTIFICATION_TYPES` + `PushSettingsPanel` `NOTIFICATION_TYPES` both gain `'group-comment'` entry (still the dual-update rule). 11 new e2e tests in `groups.spec.js` for the new endpoints. Migration roundtrip verified locally. Commit `ed03fe0`. **Tier 18 Chunk 4 — Friends' picks visibility** (2026-05-26). Surfaces every friend's pick on every game inside a ±30-day window. Two consumers: per-card collapsed panel inside GameCard + global flat list in PicksHistory's new "Friends" tab. New endpoint `GET /api/picks/friends?gameId=<uuid>` ([routes/picks.js](routes/picks.js) + [services/PickService.js](services/PickService.js) `listFriendsPicks`) — UUID-regex-validated optional gameId filter, ±30-day horizon, 500-row cap. INNER JOIN against Game so date filter applies server-side. Each row server-side scored via `lib/scoring.js scorePick` honoring Tier 17 pick-time probability snapshots (so a friend who picked at 0.35 odds shows +65 even if cascade later rewrote them) and passed through `LeaderboardService.applyMasking` (so a friend who flipped to private since accepting the request appears at masked label, not username). New `DataContext.friendsPicks` slot loaded in `loadDashboard` + `revalidate`. New [src/hooks/useFriendsPicks.js](src/hooks/useFriendsPicks.js) selector memoizes `byGame: Map<gameId, FriendPick[]>` for O(1) per-card lookups. New [src/components/FriendPicksPanel.jsx](src/components/FriendPicksPanel.jsx) mounted at the bottom of every GameCard body — won = green ✓+pts; **draw = warning yellow** (not green; matches GameCard outcome badge convention); missed = "✗ Missed" (not "+0" — easier to read). New `[Mine]/[Friends]` segmented toggle on My Picks tab with shared `comparePicksByPendingThenRecent` comparator (unresolved kickoff ASC = soonest first, then resolved kickoff DESC = most-recent first). Friend dropdown filter positioned LEFT of LeaderboardFiltersBar with matching pill styling. Pill label "Friends" has NO apostrophe; section heading "Friends' Picks" KEEPS apostrophe (deliberate distinction kept stable for screen reader consistency). 9 new boundary tests in `tests/e2e/api/picks.spec.js`. Commit `fde76b9`. **Tier 18 Chunk 3 — Calendar viewer + draws filter + compact leaderboard** (2026-05-26). Replaces the original three-section "live / upcoming / completed" cascade on the Games tab with a fixed 7-day calendar strip. New [src/components/GamesCalendar.jsx](src/components/GamesCalendar.jsx) — 7-day fixed window (today − 3 → today + 3 default), `grid grid-cols-7` chip strip, ±7-day arrow paging via `windowIndex`, URL `?date=YYYY-MM-DD` sync via `history.replaceState`. URL date that sits outside default window pre-shifts `windowIndex = Math.round(diffInDays(today, urlDate) / 7)` so chip is visible on first paint. "Back to today" pill in card header (cyan with pulsing red dot when `liveToday` — any in-progress game today regardless of window). Chip styling: day-number rendered in cyan via inline `style={{ color: 'rgb(34, 211, 238)' }}` to bypass any CSS conflicts; today's chip carries `border-accent/40` even when not selected; chips show game count + live red dot when `meta.hasLive`. Empty days render `EmptyState` with day-aware copy. `useGames` selector gained exported `dayKey(value)` helper (en-CA `YYYY-MM-DD` via `Intl.DateTimeFormat`) + memoized `byDay: Map<string, Game[]>` for O(1) per-day lookups. PicksHistory gained Draws filter chip (predicate `r.status === 'draw'`). `LeaderboardCard` gained compact mode via new `friendUserIds` prop — shows top-3 + self + friends with "… N more players" dividers + `[Show all N players]` / `[Show fewer]` toggle (`buildCompact` helper). DashboardView dropped CompletedSection/showCompleted/renderGameSection — all replaced by GamesCalendar. Commit `5c4c315`. **Tier 18 Chunk 1 — Chrome polish (clickable wordmark, PWA name, mobile safe-area)** (2026-05-26). Three small UX fixes. (1) BANTRYX wordmark in DashboardView top utility bar becomes a clickable home-button when authed — destructured `browseAsGuest` from useAuth, wordmark renders as button when `user && !browseAsGuest` calling `setView('games')`; cyan with `hover:text-white` only when `view !== 'games'` (stays cyan on the Games tab). (2) PWA manifest name shortened to `"Bantryx"` (was `"Bantryx — ScoreCast"`) in [vite.config.js](vite.config.js). (3) New `.pt-safe` utility in [src/index.css](src/index.css): `padding-top: max(0.5rem, env(safe-area-inset-top))`; applied to Sidebar's DialogPrimitive.Content alongside the existing `.safe-bottom` so iPhone notch + home indicator don't eat content. Mobile flex restructured to 3-slot. Commit `01c15cc`. **Tier 18 Chunk 2 — Live-score cadence upgrade for paid football-data.org TIER_ONE** (2026-05-23). User upgraded the football-data.org plan; probe of `GET /v4/competitions/PL` returned `x-requests-available-minute: 19` after 1 call ⇒ **20 req/min** budget (was 10 on free). Probe also confirmed `minute` / `injuryTime` are still NOT exposed at this tier (only `score.{winner,duration,fullTime,halfTime}` come back on a live Brasileiro match) — so the client-side `useMatchMinute()` estimate from Tier 4b stays in. Entitled to TIER*ONE plan on 12 competitions (PL, ELC, CL, BL1, FL1, SA, PD, DED, PPL, BSA, EC, WC). Three-file change: (1) [lib/footballApi.js](lib/footballApi.js) — `FREE_TIER_LIMIT_PER_MINUTE = 10` → `RATE_LIMIT_PER_MINUTE = Number(process.env.FOOTBALL_DATA_RATE_LIMIT) || 20`, the bail-at-`<=1`-remaining guard stays unchanged (plan-agnostic, always reserves 1 slot for admin syncs); (2) [server.js](server.js) — `LIVE_SCORE_SYNC_CRON` default `'* \* \* \* _'`→`'_/30 \* \* \* \* _'`(6-field, every 30 s; node-cron 4.x supports it),`IN_PROGRESS_RECONCILE_CRON`default`'_/5 \* \* \* _'`→`'_/3 \* \* \* *'`; (3) [.env.example](.env.example) — document the three new optional env knobs (`FOOTBALL_DATA_RATE_LIMIT`, `LIVE_SCORE_SYNC_CRON`, `IN_PROGRESS_RECONCILE_CRON`) with the new defaults. Net effect: HT/FT updates land in ~30 s instead of ~60 s; stuck-`?status=`recovery window narrows from ~5 min to ~3 min. Steady-state cost ~4 req/min vs 20 budget = 20% utilization, leaving 16+ slots/min for admin syncs. Deliberately NOT in scope: tier enum env var (single`FOOTBALL_DATA_RATE_LIMIT`integer is simpler), Bicep param (new defaults baked into server.js cover prod without IaC reapply),`useMatchMinute()`removal (minute field still absent at TIER_ONE). Cost added: €19/mo. Verified: cron expressions validate via`cron.validate()`; `requestsAvailable()`returns 20 on fresh boot; ESLint clean; 42/42 unit tests green. Commit`e409532`. **Tier 17 — Reactive Elo cascade + JS-native inference + retire Python pipeline** (6 PRs A–F; 2026-05-23). Inverts the daily-cron probability writer into an event-driven cascade. Every captured result atomically updates both teams' Elo (`SELECT ... FOR UPDATE`on`teams`rows, inside the result-capture transaction) and asynchronously rewrites probabilities for every upcoming fixture involving either team (post-commit). Three new lib/ml/ modules carry the runtime:`eloMath.js`(pure K=20 / INITIAL=1500 / HFA=0 math; JS port of`ml/scorecast_ml/elo/engine.py`), `xgboostInference.js`(zero-dep tree walker over native XGBoost JSON,`parseBaseScore`defaults to 0 for XGBoost 2.x hex-encoded values, defensive non-finite guard),`normalize.js`(toThreeWay: clip [0.01, 0.99] → round → rebalance against largest-RAW class → nudge off (0.50, 0.00, 0.50) sentinel). New`services/PredictionService.js`holds`onResultUpdated(game, {transaction})`+`rePredictFutureFixtures({affectedTeams, leagueId})`+ per-league model cache. New`models/Team.js`+ [migration 20260522000001-create-teams.js](migrations/20260522000001-create-teams.js) →`teams`table (per-(name, leagueId) Elo state, NUMERIC(8,2)). Bootstrapped by [seeders/20260522000001-seed-teams-from-elo-history.js](seeders/20260522000001-seed-teams-from-elo-history.js) which walks the committed 32-season PL CSV history in JS (identical math to Python) — operator runs once after first deploy via`npx sequelize-cli db:seed`. `LeagueService.upsertFixture`auto-inserts newly-promoted teams at`MIN(elo)`on every fixture upsert. **PR F idempotency + reversibility**: [migration 20260523000001-games-add-elo-snapshot.js](migrations/20260523000001-games-add-elo-snapshot.js) adds`games.{homeEloPre, awayEloPre, appliedResult}`— per-game pre-match Elo snapshot (immutable after first store) + record of which result has been Elo-applied. Behavior matrix in`onResultUpdated`: same result re-saved → no-op; null → non-null first capture (snapshot live Elo, apply delta, stamp appliedResult); X → Y change (reverse prior delta against locked snapshot, apply new delta against SAME snapshot); non-null → null clear (reverse, drop snapshot + appliedResult). Round-trip A↔B↔A bit-identical, regardless of intervening team activity. **PR D aggressive cleanup**: deleted `scorecast-ml-job`Container Apps Job +`scorecast-ml`ACR repo +`ml-deploy.yml`GitHub workflow +`ml-job.bicep`Bicep module +`ml-pipeline-password`KV secret +`ml_pipeline`DB user + 24 Python files under`ml/scorecast_ml/{inference,db,features,scripts}/`+`Dockerfile`+`ONBOARDING.md`. Slimmed `ml/scorecast_ml/cli.py` from 6 subcommands to 1 (`train`only); inlined strict reconcile + 2-feature build + season split + native JSON export.`ml/requirements.txt`slimmed (dropped httpx / tenacity / psycopg / rapidfuzz / joblib / pyarrow). Production model:`lib/ml/models/PL_elo.json`(615 trees, val mlogloss 0.944). Bicep reapply param count: 7 → 5 (dropped`mlImageTag`+`mlPipelinePassword`). New test infra: `npm run test:unit` → 39 unit tests via node:test (`tests/eloMath.test.js`, `tests/normalize.test.js`, `tests/xgboostInference.test.js`). New operator scripts: [scripts/query-teams.mjs](scripts/query-teams.mjs), [scripts/find-game.mjs](scripts/find-game.mjs), [scripts/repair-test-game-elo.mjs](scripts/repair-test-game-elo.mjs), [scripts/backfill-probabilities.mjs](scripts/backfill-probabilities.mjs). **PR E live-prod bugfix**: XGBoost 2.x emits `learner_model_param.base_score` as a hex-encoded float string (`"5E-1F"`for 0.5) that JS`Number()`returns NaN for. Poisoned every logit and produced`[NaN, NaN, NaN]`cascade outputs for ~48h until`parseBaseScore` defaulted to 0 (correct for multi:softprob — base*score broadcasts equally + cancels under softmax). Full architecture deep-dive at [ARCHITECTURE.md §8.17](ARCHITECTURE.md). **PWA — installable home-screen app + Web Push** (6 chunks; 2026-05-20). Turns ScoreCast into a home-screen-installable app on Android Chromium + iOS Safari ≥16.4 with native OS push notifications. **Chunks 1-3 (shell)**: `vite-plugin-pwa` (initially `generateSW`, switched to `injectManifest` in Chunk 5); manifest.webmanifest with `display: standalone` + brand colors; icon set generated from a single [public/logo.svg](public/logo.svg) source via [scripts/generate-pwa-assets.mjs](scripts/generate-pwa-assets.mjs) using `@resvg/resvg-js` + `png-to-ico` (avoids the broken `sharp/libvips` win32-arm64 prebuild that breaks `@vite-pwa/assets-generator` on ARM Windows); apple-_ meta + viewport-fit=cover in [index.html](index.html); helmet CSP gains `worker-src 'self'` + `manifest-src 'self'`. [src/components/InstallPrompt.jsx](src/components/InstallPrompt.jsx) + [src/hooks/useStandalone.js](src/hooks/useStandalone.js) render a Chromium "Install app" button OR iOS "Share → Add to Home Screen" instructions, suppressed once installed or dismissed via `localStorage.sc_install_dismissed`. Mobile polish: [src/components/ui/Button.jsx](src/components/ui/Button.jsx) base gains `min-h-[44px] md:min-h-0` so every Button hits the 44 px WCAG 2.5.5 touch-target floor on mobile while staying compact on desktop; `.safe-bottom` utility in [src/index.css](src/index.css) for future bottom-docked UI; [src/components/CommentThread.jsx](src/components/CommentThread.jsx) reaction row gap bumped 1→2 for thumb accuracy. **Chunks 4-6 (Web Push)**: new [migrations/20260520000001-create-push-subscriptions.js](migrations/20260520000001-create-push-subscriptions.js) (`push_subscriptions` table with `ON DELETE CASCADE` + unique `(userId, endpoint)` index + `users.pushPreferences JSONB DEFAULT '{}'`); [models/PushSubscription.js](models/PushSubscription.js) + association + `UserService.cascadeDelete` extension. [services/PushService.js](services/PushService.js) — `init()` self-runs at module load, mirrors `lib/email.js`'s never-throw / graceful-no-op shape; `sendToUser(userId, type, payload)` honors `users.pushPreferences[type] !== false`, deletes subs on 410/404, increments `failureCount` on other errors with 5-strike auto-purge. [services/NotificationService.js:notify](services/NotificationService.js) fires `PushService.sendToUser` fire-and-forget AFTER the DB insert — every existing notify call site (PickService, GameService, GroupService, BadgeService, plus the friend-request notifications in [routes/friends.js](routes/friends.js)) now dispatches push for free. New [routes/push.js](routes/push.js): `GET /api/push/vapid-public-key` (anon + `publicReadLimiter`, 503 when VAPID unset), `POST /api/push/subscribe`, `DELETE /api/push/subscribe` (both auth + CSRF + zod). New `PUT /api/me/push-preferences` in [routes/me.js](routes/me.js) does a JSONB merge so partial updates don't clobber. [validation/schemas.js](validation/schemas.js) `PUSH_NOTIFICATION_TYPES` is the enum source of truth for 7 types: `pick-scored`, `badge`, `invite`, `group-join`, `odds-shifted`, `kickoff-reminder`, `friend-request`. **Chunk 5 SW + UI**: [src/sw.js](src/sw.js) (`injectManifest`) carries workbox precache + runtime caching (Google Fonts + `/api/{games,leaderboard,me,groups,leagues}` 5-min SWR) + `push` listener → `registration.showNotification(title, {body, icon, badge, tag: type, data: {link}})` + `notificationclick` → focus existing tab or `clients.openWindow`. [src/hooks/usePushSubscription.js](src/hooks/usePushSubscription.js) drives the W3C ceremony (`Notification.requestPermission()` → fetch VAPID key → `pushManager.subscribe(...)` → POST), with rollback on server-side failure so client+server never drift. [src/components/PushSettingsPanel.jsx](src/components/PushSettingsPanel.jsx) mounts in ProfileView Settings — five states: iOS install-gate, browser-unsupported, permission-denied, default/unsubscribed master toggle, subscribed master + 7 per-type checkboxes with optimistic update + rollback. [src/components/NotificationBell.jsx](src/components/NotificationBell.jsx) drops 30 s → 5 min poll when `navigator.serviceWorker.controller != null`. **Chunk 6 cron + tests + infra**: [lib/jobs/sendKickoffReminders.js](lib/jobs/sendKickoffReminders.js) — every 15 min, finds scheduled games kicking off in 15-30 min with `kickoffReminderSentAt IS NULL`, fires `kickoff-reminder` notifications for every Pick (push fan-out via PushService), stamps `games.kickoffReminderSentAt`. Register via `scheduler.register('sendKickoffReminders', '_/15 \* \* \* _', ...)`with`KICKOFF*REMINDER_CRON`env override. [tests/e2e/api/push.spec.js](tests/e2e/api/push.spec.js) — 12 boundary tests across all four push endpoints (anon vapid-key, subscribe/unsubscribe 201/200/401/403/400, push-preferences PUT 200/401/403/400/enum-mismatch). [tests/e2e/helpers/api.js](tests/e2e/helpers/api.js) gains`clearPushSubscriptions(userIds)`. [tests/e2e/fixtures/seed.js](tests/e2e/fixtures/seed.js) sets `pushPreferences: {}`explicitly for parity. **Infra**: [infra/modules/app.bicep](infra/modules/app.bicep) + [infra/main.bicep](infra/main.bicep) gain`vapidPublicKey`+`vapidSubject`params, an always-on`vapid-private-key` KV secret reference, and three new env vars (`VAPID_PUBLIC_KEY`value,`VAPID_PRIVATE_KEY`secretRef,`VAPID_SUBJECT`value). KV must be pre-seeded BEFORE the first Bicep reapply that wires push (same bootstrap pattern as`jwt-secret`). **Verified locally**: server boots cleanly with NO VAPID env vars set → PushService logs the no-op warn, `/api/push/vapid-public-key`returns 503 with`{"error":"Push notifications are not configured on this server"}`, `POST /api/push/subscribe`still 403's anon for CSRF (auth boundary holds),`dist/sw.js`emits at 47 KB with push handler intact. Doc sweep: ARCHITECTURE.md gains §8.23 PWA + Web Push (full lifecycle diagrams). **Live-score reconcile hardening** (2026-05-19). Incident: AFC Bournemouth vs Manchester City (sourceId 538145) ended 1-1 at 22:25 UTC but stayed locally stuck at HT 1-0 status='in-progress' for 40+ min in both dev and prod. Root cause via prod log analysis + an in-cluster`getMatchesByIds`probe: football-data.org's`?status=LIVE,IN_PLAY,PAUSED`filter went stale relative to the canonical`?ids=538145`endpoint (which correctly returned`FINISHED`+`DRAW`+1-1 with `lastUpdated: 2026-05-19T22:25:33Z`). The 1-min `syncLiveScores` job faithfully mirrored the stale filter; the existing reconcile pass explicitly excluded the game because it WAS in the LIVE response (`sourceId NOT IN [...liveSourceIds]`), so we never escalated to `?ids=`. Three fixes shipped together: (1) new [lib/jobs/reconcileInProgressGames.js](lib/jobs/reconcileInProgressGames.js) — sweeps every local `status='in-progress'`game with a sourceId every 5 min via`getMatchesByIds`, ignoring LIVE-filter membership; registered in [server.js](server.js) with `IN_PROGRESS_RECONCILE_CRON`env override; targeted`reconcileInProgressGames: caught stale-upstream finish via ?ids=`log line per transition for operator visibility. (2) [services/GameService.js](services/GameService.js)`applyLiveUpdate`rewritten to wrap compute+save in a single transaction with`SELECT ... FOR UPDATE`on the game row — closes the row-level race when the 1-min and 5-min jobs fire at xx:00/xx:05 alignments (without this, both jobs could load a stale snapshot at the same instant and the second save would overwrite the first, e.g. stale 1-min job regressing status='in-progress'+scores back after the 5-min job correctly set FINISHED). (3) Finished-status guard in`applyLiveUpdate`: once `fresh.status === 'finished'`, any upstream snapshot whose `apiMatch.status`is NOT`FINISHED`or`AWARDED`is treated as a stale lie — returns`changed: false`and logs`applyLiveUpdate: ignored stale non-FINISHED upstream snapshot for already-finished game`. Verified locally against the dev DB's stuck game by mocking `footballApi.getMatchesByIds`with the canonical upstream response — pass/idempotency/guard all green (SQL trace confirms`SELECT ... FOR UPDATE`is firing). Cost added: ≤1 extra request per 5 min within the 10-req/min free-tier budget. Note: the dev environment's stuck state also stems from`FOOTBALL_DATA_API_KEY`being unset in`.env`, which silently no-ops both cron jobs — already documented elsewhere. **Security hardening batch + per-endpoint API test suite** (2026-05-18). Twelve security fixes against the [review-tier-4b-plan-optimized-falcon.md](C:/Users/vinde/.claude/plans/review-tier-4b-plan-optimized-falcon.md) plan, then ~250 new Playwright tests under `tests/e2e/api/`to lock in the boundaries. **H1**:`app.set('trust proxy', 1)`in [server.js](server.js) so`req.ip`resolves to the real client IP through Cloudflare → Azure ingress (was the proxy IP, broke every per-IP rate limiter). **H2**: dummy`bcrypt.compare`against`LOGIN_DUMMY_HASH`in [routes/auth.js](routes/auth.js) — login response time is now constant regardless of whether the username exists, closing the timing-based enumeration channel. **H3**:`currentPassword`required on`PATCH /api/me/email`+`POST /api/me/2fa/setup`(validation/schemas.js`setEmailSchema`/`totpSetupSchema`) so a stolen 15-min access JWT alone can't pivot into account takeover; old-email notification fires before the new email overwrites. **M1**: `/api/auth/forgot-password`token INSERT + email send moved to`setImmediate(...)`so the 204 response is dominated only by the user lookup that runs in all branches — verified / unverified / nonexistent now respond in equal time. **M3**:`lib/instrument.js`Sentry init gains explicit`sendDefaultPii: false`+`maxBreadcrumbs: 50`+ a`beforeSend`hook that redacts any key matching`password|secret|token|recovery|otp|totp|cookie|set-cookie|authorization|csrf|api[-*]?key`from`request.{data,headers}`, `extra`, `contexts`, and `breadcrumbs[].data`; `request.cookies`dropped entirely. **M4**:`jwt.verify(..., { algorithms: ['HS256'] })`pinned on all four call sites — [middleware/auth.js](middleware/auth.js), [middleware/optionalAuth.js](middleware/optionalAuth.js), [routes/auth.js](routes/auth.js) 2FA challenge verify, [routes/client-errors.js](routes/client-errors.js). **M5**: new`POST /api/me/password`endpoint ([routes/me.js](routes/me.js)) +`setPasswordSchema`+`ChangePasswordPanel`in [src/components/ChangePasswordPanel.jsx](src/components/ChangePasswordPanel.jsx) mounted above the 2FA section in ProfileView. Bcrypt-compares`currentPassword`, saves new password (Sequelize beforeUpdate re-hashes), `revokeAllUserRefreshTokens`, then `setAuthCookies`to mint a fresh pair — calling client stays signed in, every other refresh-bearing device is kicked out. **L3**:`Permissions-Policy: camera=(), microphone=(), geolocation=(), payment=()`middleware in [server.js](server.js) right after helmet. **L4**:`bodyParser.json({ limit: '32kb' })`(was 100KB default; largest legit payload is the client-error report at ~17KB). **L5**: recovery-code verification in`/auth/2fa/verify`+`/me/2fa/disable`switched from early-exit`for`to`Promise.all(codes.map(bcrypt.compare))`— no early-exit timing leak on which slot matched, total latency drops from N bcrypts to one. **L6**:`displayName`+`bio`in`editProfileSchema`reject U+202A–U+202E (bidi explicit), U+2066–U+2069 (bidi isolates), U+200B/U+200C (ZWSP/ZWNJ), U+200E/U+200F (LRM/RLM), U+FEFF (BOM), U+0000–U+001F (C0), U+007F–U+009F (DEL + C1). ZWJ (U+200D) is intentionally NOT in the block list so ZWJ-emoji like 👨‍💻 still work. **L8**: CI gains`npm audit --audit-level=high --omit=dev`; `.github/dependabot.yml` opens weekly grouped PRs for npm prod/dev + pip (ml/) + github-actions + docker. Then the **per-endpoint API test suite** under [tests/e2e/api/](tests/e2e/api/) — one spec per route file (`auth.spec.js`, `me.spec.js`, `games.spec.js`, `picks.spec.js`, `comments.spec.js`, `groups.spec.js`, `friends.spec.js`, `leaderboard.spec.js`, `notifications.spec.js`, `users.spec.js`, `leagues.spec.js`, `admin.spec.js`, `client-errors.spec.js`, `health.spec.js`) covering happy path + auth-required 401 + admin-required 403 + CSRF-required 403 + zod-validation 400 + ownership 403/404 + missing-id 404 for every one of the 68 endpoints. New helper module [tests/e2e/helpers/apiAssertions.js](tests/e2e/helpers/apiAssertions.js) collapses per-test boilerplate. The pre-CSRF-middleware ordering insight: `assertUnauthorized`seeds an`sc_csrf`cookie via a throwaway GET so state-changing routes' auth boundary lands on`authMiddleware`(401) instead of being absorbed by CSRF (403). Plus a UI smoke at [tests/e2e/change-password-panel.spec.js](tests/e2e/change-password-panel.spec.js) — drives the ChangePasswordPanel through the browser (expand / show-hide toggle / mismatch error / wrong-password error / happy path), restores alice's password in afterEach. Full-suite now: 270 tests across 22 spec files (was ~20 across 9). **Leaderboard league + season filter** (2026-05-18). Both overall and per-group leaderboards now accept optional`?leagueId=&seasonId=`query params;`GET /api/leaderboard`validates via`leaderboardQuerySchema` (`validation/schemas.js`) applied **inline** in [routes/leaderboard.js](routes/leaderboard.js) because the shared `validate()`middleware only handles`req.body`. Builders `lib/groups.js buildGroupLeaderboard(groupId, {leagueId, seasonId})`+`lib/users.js buildUserSummary({leagueId, seasonId})`add a`where: gameWhere`clause to`Game.findAll()`; the existing in-memory pick-loop's `if (!gameById.has(pick.gameId)) continue;`guard then naturally drops out-of-scope picks from both numerator AND denominator →`winRate`scopes automatically. Users with zero in-scope picks stay listed at`points: 0`. Cache key shape extended via `LeaderboardService.buildKey(scope, {leagueId, seasonId})`to`overall:l:<id|_>:s:<id|_>`and`group:<groupId>:l:<id|_>:s:<id|_>`(unfiltered =`_:_`so requests without filters share the key). Added`lib/leaderboardCache.js invalidatePrefix(prefix)`(walks the Map clearing every key matching`prefix`exactly or starting with`prefix:`) — required because one logical scope now spans many keys. `GroupService.{acceptInvite,joinPublic,leave,deleteGroup}`switched from`invalidate('group:${id}')`to`invalidatePrefix('group:${id}')`. Frontend: new [src/components/LeaderboardFiltersBar.jsx](src/components/LeaderboardFiltersBar.jsx) (mirror of GameFiltersBar; writes to `?lbLeague=<code>&lbSeason=<year>`URL keys distinct from games-view's`?league=&season=`); new `DataContext.leaderboardFilters`slot +`applyLeaderboardFilters()`mutator threaded through`refreshLeaderboard`. Filter bar mounts on the Leaderboard tab (one bar above both `LeaderboardCard`+`GroupLeaderboardCard`) AND the My Picks tab — single global "stats scope" filter. `PicksHistory`scopes rows client-side by`game.leagueId`/`game.seasonId`(no server-side picks-filter endpoint). Both leaderboard cards + PicksHistory gain a "Filtered" pill near their title when scope is active, plus a scope-aware empty state ("No picks in this scope yet"). Privacy masking layer (Tier 8.6) unchanged — cache rows stay viewer-agnostic, row shape stays the same. Build: 3.4s clean; PicksHistory chunk bumped from ~6KB → 7.8KB. **Draw scoring** (partial credit on drawn matches; 2026-05-17). End-to-end tier that turns drawn matches from a leaderboard dead-end ("everyone who picked a side gets zero") into a partial-credit moment with the math`pts = round(P*d × opposite_team_prob / (P_h + P_a) × 100)`. Backend: [migration 20260518000008-games-add-draw-scoring.js](migrations/20260518000008-games-add-draw-scoring.js) adds `games.drawProbability DECIMAL(3,2) NOT NULL DEFAULT 0`+ extends the`games.result`enum to`('home', 'away', 'draw')`via`ALTER TYPE ... ADD VALUE IF NOT EXISTS 'draw'`; [models/Game.js](models/Game.js) gains the field + enum value; [validation/schemas.js](validation/schemas.js) `createGameSchema`/`updateGameSchema`accept optional`drawProbability`with the sum-to-1`.refine()`updated to validate the trio (treating absent draw as 0 for backward compat);`resultSchema`+`bulkGameSchema.result`accept`'draw'`; [lib/scoring.js](lib/scoring.js) `scorePick`gains the draw branch (mirrors src/utils/scoring.js); [services/GameService.js](services/GameService.js)`bulkSetResult`allows`'draw'`+ a shared`pickResultTitle`helper formats the bell notification with three branches (Won / Drew / Missed) instead of the old binary; [lib/fixtureStatus.js](lib/fixtureStatus.js)`deriveResultFromFixture`flips upstream`DRAW`(and score-equality fallback) from`null`to`'draw'`. ML pipeline: [ml/scorecast_ml/inference/normalize.py](ml/scorecast_ml/inference/normalize.py) gains `Triple`+`to_three_way`+`round_and_rebalance_triple`(largest RAW class absorbs the rounding residual to preserve model ordering through ties) +`nudge_off_triple_sentinel`against the new`(0.50, 0.00, 0.50)`post-migration sentinel; [ml/scorecast_ml/inference/predict.py](ml/scorecast_ml/inference/predict.py) calls`to_three_way`instead of`to_two_way`, emits `draw_out`alongside`home_out`/`away_out`; [ml/scorecast_ml/db/queries.py](ml/scorecast_ml/db/queries.py) SELECT now reads `drawProbability`so the writer's sentinel check sees all three; [ml/scorecast_ml/db/writer.py](ml/scorecast_ml/db/writer.py) PUT payload includes`drawProbability`+ sentinel updated. Legacy 2-class`to_two_way`+`(0.50, 0.50)`sentinel preserved for ablation scripts but off the live write path. Plus a calibrator-edge clip in [ml/scorecast_ml/train/model.py](ml/scorecast_ml/train/model.py)`ModelBundle.predict_proba`— every calibrated value floored at 0.01 / ceiling at 0.99 before renormalization so isotonic's lower-bound 0 mapping (caught Arsenal-vs-Burnley emitting literal`100% / 0% / 0%`in the wild) never reaches the DB. Frontend: [src/utils/scoring.js](src/utils/scoring.js) mirrors the draw branch in`scorePick`; `pickStatus`returns a new`'draw'`state;`expectedDrawPoints`swapped to the new formula and returns`null`for unconfigured games so`PayoutMatrix`shows`+x`/`+y`placeholders rather than misleading`+0`. [src/components/GameCard.jsx](src/components/GameCard.jsx) `outcomeBadge`+`LockedPickChip`gain Drew branches (warning tone);`ScoreboardBody.winningSide`narrowed to home/away so draws leave both team boxes un-dimmed. [PicksHistory.jsx](src/components/PicksHistory.jsx) + [ProfileView.jsx](src/components/ProfileView.jsx)`statusBadge`/`recentPickStatus`gain Drew branches. Admin: [src/components/admin/GameManager.jsx](src/components/admin/GameManager.jsx) create + edit forms gain a Draw probability input; per-row + bulk action bars gain a`Draw`button (warning tone); read-only row shows`H% / D% / A%`triple; result pill renders`Result: Draw`. Tests: 5 new `to_three_way`invariants in [ml/tests/test_normalize.py](ml/tests/test_normalize.py) + 1 calibrator-clip test in [ml/tests/test_calibration.py](ml/tests/test_calibration.py) (48/48 ML tests green; e2e existing assertions unaffected since they're all home/away). Operator runbook for ad-hoc Container Apps Job runs with`--overwrite-existing`(etc.) added to [ml/README.md](ml/README.md) — the`az containerapp job start --args`flag can't pass`--`-prefixed values, so manual override requires `az rest`with a JSON body that carries`image`, `resources`, AND `env` over from the deployed template (full container replace, not merge). Cost added: $0/mo. **ML Probability Pipeline — Phase 3** (Azure deployment; 2026-05-17; flipped to daily 02:30 UTC on 2026-05-18). Production pipeline now runs as a scheduled Azure Container Apps Job (`scorecast-ml-job`) — daily 02:30 UTC, 30 min ahead of the Node app's 03:00 UTC fixture sync. (Original launch was Thursdays-only; switched to daily so newly-synced fixtures get probabilities within 24h.) Three new pieces: (1) [ml/Dockerfile](ml/Dockerfile), a 3-stage build (`python:3.14-slim`base + libgomp1/tini → train stage that runs`python -m scorecast_ml train`against the committed CSV corpus producing a deterministic`seed=42`model bundle → non-root runtime layer with CMD`predict-and-write --league PL --horizon-days 7`); (2) [infra/modules/ml-job.bicep](infra/modules/ml-job.bicep), a Schedule-trigger Container Apps Job with the same managed-identity RBAC pattern as `migrate-job.bicep`(AcrPull + Key Vault Secrets User), provisioning the`ml-pipeline-password`KV secret from the new`mlPipelinePassword`Bicep param + consuming`database-url`+`ml-pipeline-password`via secret refs; (3) [.github/workflows/ml-deploy.yml](.github/workflows/ml-deploy.yml), a path-filtered build-and-push workflow on`ml/\*\*`changes pushing to a new ACR repo`scorecast-ml`(separate from the Node`scorecast`repo so the two CDs never collide). Wired into [infra/main.bicep](infra/main.bicep) as a new`mlJob`module call alongside`migrateJob`; `apiBaseUrl`falls back to the Container App FQDN when`customDomain`is empty. CSV training corpus (32 PL seasons, ~3 MB, public-domain) flipped out of gitignore via a`!ml/data/raw/*.csv`negation rule so the Docker build has the data without a network fetch.`ml-pipeline-password`joins`pgAdminPassword`as a required param on every Bicep reapply (full list now:`pgAdminPassword`, `mlPipelinePassword`, `customDomain`, `customDomainCertId`). Day-to-day CD unaffected — the Node `deploy.yml`and the new`ml-deploy.yml`both only update image tags, never run Bicep. Cost added: $0/mo (Consumption-profile Job billed per run-second; typical execution <60s × 0.5 vCPU × 1 GiB = sub-cent per week). Operator runbook in [ml/README.md](ml/README.md#phase-3--deployment-shipped) covers initial deploy, password rotation, and retrain flow. **ML Probability Pipeline — Phase 2** (isotonic calibration + production retrain; 2026-05-17). Added per-class`IsotonicRegression`calibrators fit on the val set, attached to`ModelBundle.calibrators`, applied automatically inside `predict*proba`with row renormalization (isotonic per-class doesn't preserve the simplex). Train CLI gained`--no-calibration`flag (default: on) + reports both`val_uncalibrated`and post-calibration`val`metrics for transparency. Bundle meta JSON gains`calibrated`boolean. 8 new pytest cases in`ml/tests/test_calibration.py`cover sum-to-1, [0,1] range, raw-bypass, legacy-bundle fallback, and a self-calibrating sanity check. Live model retrained on the new production split: 15-season train (2009/10 -> 2023/24, 5700 matches) + 1-season val (2024/25, 380 matches; consumed by both early stopping + isotonic fit) + 25/26 in-progress held-out test via`scripts/backtest_2526.py`(361 OOS matches). Aggregate metrics on 25/26 move modestly (mlogloss 1.037 -> 1.032, accuracy 47.6% -> 47.9%) but calibration shape improves significantly: the 70-80% confidence bucket overconfidence drops from -7pp to -2pp deviation (from the diagonal); the <40% bucket fixes from -7pp underconfident to +2pp (well-calibrated); the model now reaches >80% confidence on top calls (didn't pre-calibration, e.g. Arsenal-vs-Sunderland at 0.89). For ScoreCast's`(1 - p_winning) × 100`scoring formula this is the structural win — payouts on correct picks now match real probabilities. Old 5-season uncalibrated model archived at`ml/data/models/PL_2025-05-25_5season_uncal.joblib`for diffability.`load_latest_bundle`strict-match unchanged (suffixed variants still ignored).`scripts/backtest_2526.py`updated to call`load_latest_bundle`instead of the hardcoded`\_hfa0`path. **ML Probability Pipeline — Phase 1** (PL only, manual; 2026-05-17). Standalone Python project under [ml/](ml/) producing`(homeProbability, awayProbability)`for upcoming fixtures via Elo + XGBoost and writing them through`PUT /api/admin/games/:id`(audit-logged). Activates the existing`(1 - p) × 100`scoring formula — picking a correctly-predicted 20% upset now pays 80 pts vs the previous flat 50 pts under default`(0.50, 0.50)`. Built 8 sub-packages in `ml/scorecast_ml/`: `ingest`(Football-Data.co.uk CSV downloader; stdlib-csv parser tolerates ragged historical rows),`reconcile`(51 PL team aliases + rapidfuzz fallback with loud-error path on unknowns),`elo`(vanilla engine +`home_field_advantage=0`default +`promoted_team_strategy='min_rating'`), `features`(11-column matrix computed as-of match date via`compute_form`'s `date < as_of`filter),`train`(XGBoost`multi:softprob`+ time-based split +`--train-from-season`window +`--model-suffix`for A/B),`inference`(3-class→2-class draw redistribution + DECIMAL(3,2) re-balance +`(0.50, 0.50)`sentinel-avoidance),`db`(psycopg reader + HTTP writer mirroring`apiLogin`cookie+CSRF flow). 34 pytest smoke tests covering Elo determinism (incl. promoted-team strategy), normalize sum-to-1 invariants over a 100-pt grid, reconcile manual/fuzzy/error paths. Backtests: 5-season train (2004/05-2008/09) + 15-season test (2010/11-2024/25, 5700 OOS matches): **mlogloss 0.992 vs baseline 1.065** (-0.073), **accuracy 51.9% vs 44.9%** (+7 pp). Walk-forward 25/26 from live DB (361 matches): mlogloss 1.037 vs baseline 1.080 (-0.043), accuracy 47.6% vs 42.4% (+5.3 pp); mild overconfidence above 70% (Phase 2's calibration target). End-to-end smoke test verified:`ml_pipeline`admin user provisioned, dry-run + real PUT wrote 18 current 25/26 fixtures with 0 failures, audit-log captured all 18 with full`after`payloads, idempotency (skip-non-sentinel) works,`--overwrite-existing`flag bypasses. Three CLI demo scripts at [ml/scripts/](ml/scripts/):`demo_predict_one.py`(single Liverpool-vs-Arsenal with feature trace),`compare_hfa.py`(HFA ablation),`backtest_2526.py`(walk-forward season eval from DB). Per-league onboarding playbook at [ml/ONBOARDING.md](ml/ONBOARDING.md) covers La Liga / Bundesliga / Serie A / Ligue 1. Deep-dive in [ARCHITECTURE.md §8.17](ARCHITECTURE.md). Cost added: $0/mo (Football-Data.co.uk is free; football-data.org free tier reused). **Tier 4b** — External football data + leagues/seasons + audit log (3 chunks; 2026-05-16/17). **Chunk 1**: [lib/footballApi.js](lib/footballApi.js) (football-data.org v4 client, sliding-window rate limit, in-process [lib/cache.js](lib/cache.js)) + [lib/fixtureStatus.js](lib/fixtureStatus.js) (shared status mapping + result derivation) + new`leagues`/`seasons` tables (migrations [20260518000001](migrations/20260518000001-create-leagues.js) / [20260518000002](migrations/20260518000002-create-seasons.js)) seeded with PL (active) + WC (inactive) + games extensions (`leagueId`/`seasonId`/`sourceId`/`status`enum /`homeScore`/`awayScore`/`kickoffTz`) via migrations [3](migrations/20260518000003-games-add-league-season-source.js) / [4](migrations/20260518000004-games-status-enum.js) + [services/LeagueService.js](services/LeagueService.js) (CRUD + `syncFixtures(leagueId)`upserting by`(leagueId, sourceId)`) + admin endpoints `GET/POST/PUT/DELETE /api/admin/leagues`+`POST /api/admin/leagues/:id/sync`wrapped in [routes/admin.js](routes/admin.js) + [src/components/admin/LeagueManager.jsx](src/components/admin/LeagueManager.jsx). **Chunk 2**: [lib/scheduler.js](lib/scheduler.js) (node-cron +`pg_try_advisory_lock(crc32(jobName))`for multi-replica safety) + [lib/jobs/syncFixtures.js](lib/jobs/syncFixtures.js) (daily`0 3 \* \* *`UTC, iterates active leagues) + [lib/jobs/syncLiveScores.js](lib/jobs/syncLiveScores.js) (60-s global`?status=LIVE,IN*PLAY,PAUSED`poll, filters to active leagues, plus a **reconcile pass** via`?ids=`for in-progress local games that fell off the LIVE filter — catches the FINISHED transition) +`GameService.applyLiveUpdate`(transactional status/score/result transition; notify + badge + cache fan-out outside the transaction per Tier 5.3 invariant). Plus a **live-minute estimate** that works around the free-tier's missing`minute`field — added`games.halfTimeReached BOOLEAN`+`games.phase VARCHAR(20)`(migration [5](migrations/20260518000005-games-add-live-phase.js)) populated from`score.halfTime`+`score.duration`, consumed by [src/utils/time.js](src/utils/time.js) `useMatchMinute()`which clamps to`"HT"`/`"ET"`/`"PEN"`/`"90'+"`in the obvious cases. [src/components/GameCard.jsx](src/components/GameCard.jsx) now shows a pulsing red`Live · 67'`pill + per-team tabular scores when`status='in-progress'`, and a `Draw`/`Final`label for finished matches.`GameService.setResult`flips`status`alongside`result`so manual admin entries stay consistent with synced rows.`useGames`selector now buckets by`status`first, falling back to`result`(so synced draws land in`completed`instead of`live`). Bug-fix during verification: football-data.org rejects repeated `?status=X&status=Y`params (wants comma-separated), and the live-score job was missing the reconcile pass — both fixed in [lib/footballApi.js](lib/footballApi.js) and [lib/jobs/syncLiveScores.js](lib/jobs/syncLiveScores.js). **Chunk 3**: audit log infrastructure — [middleware/auditLog.js](middleware/auditLog.js)`auditMutation(action, entityType)`wraps every`/api/admin/*`mutation; [services/AuditLogService.js](services/AuditLogService.js) records via`res.on('finish')`with 4KB payload truncation (never throws back into the request lifecycle);`audit*log`table (migration [6](migrations/20260518000006-create-audit-log.js)) with`actorUserId`SET NULL on user delete so history survives admin removal;`GET /api/admin/audit-log`paginated; [src/components/admin/AuditLog.jsx](src/components/admin/AuditLog.jsx) admin tab with per-row`<details>`payload preview. Plus league/season picker — public`GET /api/leagues`(anon-safe via`publicReadLimiter`), `GET /api/games?leagueId=&seasonId=`filters with UUID-shape guard, [src/components/GameFiltersBar.jsx](src/components/GameFiltersBar.jsx) dropdown with`?league=PL&season=2026`URL sync via`history.replaceState`(no router needed),`DataContext.gameFilters`so refreshes preserve the active filter. Plus final tightening migration [7](migrations/20260518000007-games-tighten-league-not-null.js):`games.leagueId NOT NULL`(idempotent; backfills any orphan rows into a synthetic`Legacy / Imported`league + current-year season before the ALTER).`bulkGameSchema.ids`cap raised 100 → 500. Validation middleware now surfaces the first zod issue as`error`(was the generic "Invalid request body"). Total cost added: $0/mo. Launch scope is PL + WC (BSA + CLI also added manually for live-match testing — admin can deactivate from the UI). **Tier 8.6** — Profile privacy. New`users.profileVisibility` ENUM (`'public'`/`'friends'`/`'private'`; default `'public'`) via [migration 20260516000003-users-add-profile-visibility.js](migrations/20260516000003-users-add-profile-visibility.js). [services/UserService.js](services/UserService.js) gained `getProfileByUsername({username, viewer})`— extracts the ~100 LOC of profile assembly that wasn't picked up in Tier 13, plus a visibility gate (admin/self/public/friends-with-accepted-friendship → allow; else`errors.notFound`). [services/LeaderboardService.js](services/LeaderboardService.js) gained `getOverallForViewer(viewer)`+`getForGroupForViewer(groupId, opts, viewer)`— masking layer that projects cached viewer-agnostic rows through`applyMasking(rows, {viewerId, viewerIsAdmin, friendIds, exemptIds})`. Cached rows now carry `profileVisibility`(added to`lib/users.js` `buildUserSummary`+`lib/groups.js` `buildGroupLeaderboard`). [lib/friends.js](lib/friends.js) gained `getViewerFriendIdSet(viewerId)`— one query per request. [routes/me.js](routes/me.js)`PUT /api/me`now accepts`profileVisibility`and invalidates the leaderboard cache when`displayName`or`profileVisibility`actually changes (fixes a pre-existing 30-s lag for`displayName`too). Frontend: [src/components/ProfileView.jsx](src/components/ProfileView.jsx) Settings now renders a "Privacy" panel with three radio options; [src/components/LeaderboardCard.jsx](src/components/LeaderboardCard.jsx)`LeaderboardRow`honors`entry.isMasked`(italic + private chip + click suppression); [src/components/ProfileDrawer.jsx](src/components/ProfileDrawer.jsx) renders a "This profile is unavailable" sheet when fetching a gated profile fails; [src/contexts/DataContext.jsx](src/contexts/DataContext.jsx) gained`profileError`state so the drawer can distinguish "loading" from "blocked". New [tests/e2e/profile-privacy.spec.js](tests/e2e/profile-privacy.spec.js) locks down 5 invariants — friends-only non-friend → 404, friends-only friend → full payload, private non-admin → 404, private admin → full payload, leaderboard masking by viewer relationship. All 9 specs (20 tests) green. **Tier 9-followup** — Bicep ↔ custom domain reconciliation. [infra/main.bicep](infra/main.bicep) gained`customDomainCertId`+`useAzureDns`params; [infra/modules/app.bicep](infra/modules/app.bicep) now writes the`customDomains: [{name, bindingType:'SniEnabled', certificateId}]`array onto the Container App ingress when`customDomain`is non-empty.`CORS_ORIGINS`+`PUBLIC_APP_URL`env vars were already wired to flip on`customDomain`. The `dns.bicep`module — which would have created an empty Azure DNS zone for`bantryx.com`— is now gated behind`useAzureDns=false`(default) since DNS is on Cloudflare. Verified via`az deployment group what-if`: zero net resource changes against the live state (11 false positives from reference-vs-literal + platform-managed defaults; the customDomain binding itself is captured). A full IaC reapply now preserves `bantryx.com`+ cert + env vars. **Tier 11 Chunk 4** — foundational accessibility + first-run onboarding tour. New users land with`users.onboardingCompletedAt = NULL`([migration 20260516000001-users-add-onboarding.js](migrations/20260516000001-users-add-onboarding.js) + model field) and see [src/components/OnboardingTour.jsx](src/components/OnboardingTour.jsx) — a 4-step Radix Dialog walking through picks → scoring → leaderboard → groups. Skip + Done both`POST /api/me/onboarding-completed`(idempotent, preserves existing timestamp). [tests/e2e/fixtures/seed.js](tests/e2e/fixtures/seed.js) pre-completes the tour for seed users so existing flows aren't blocked; runtime-registered users in`pick-and-result.spec.js`get dismissed via the new [tests/e2e/helpers/auth.js](tests/e2e/helpers/auth.js)`dismissOnboardingTour()` helper. New a11y infrastructure: [src/lib/a11y.js](src/lib/a11y.js) (`useReducedMotion()`+`useFocusOnRouteChange()`); skip-to-content link in App.jsx (visible on focus, targets `#main`); `<nav aria-label="Primary navigation">`wrapping the sidebar tablist;`<main id="main">`landmarks on DashboardView/AuthView/SkeletonView;`role="alert"`on TwoFactor inline errors. New public [ACCESSIBILITY.md](ACCESSIBILITY.md) documents WCAG 2.1 AA targets + known gaps. Tour uses`useReducedMotion`to skip its dialog animation when the OS requests reduced motion. All 15 Playwright tests green. **Tier 11 Chunk 3** (CSS-only iOS Safari fix) — 16 px minimum font-size on every editable form field at`<`768 px so tapping search/login/comment/admin inputs no longer auto-zooms the viewport (and leaves it stuck zoomed after blur). Single CSS rule in [src/index.css](src/index.css) using`!important`to win over the per-component Tailwind`text-sm` utilities. Desktop unaffected. **Back-to-landing pill + logout-to-landing** — anonymous visitors now see a third pill button (`[← Home]`) in the top utility bar next to `[Sign in]`+`[Sign up]`. Clicking it resets `browseAsGuest`+`showAuth`so the visitor returns to`<Landing />`. `performLogout`was also reshaped: it now resets BOTH`browseAsGuest=false`and`showAuth=false`AND clears`localStorage.sc_visited`so an explicit sign-out always lands on the marketing landing page (and stays there on refresh) — a fresh start. Earlier "logout → anon dashboard" behavior is gone; explicit logout is treated as a fresh visit. The BANTRYX wordmark in the top bar stays a decorative`<h2 aria-hidden>`for everyone (the earlier attempt at making it a back-button was reverted in favor of the explicit pill). E2E`logoutViaUI`post-logout sentinel`getByRole('button', { name: 'Sign in', exact: true })`matches the Landing's "Sign in" CTA. All 15 tests green. **Anonymous browse mode (standalone, pre-Tier 11)** — visitors can explore Games / Rankings / Public Groups / comments / public profiles / search without an account; only actions require sign-in. Backend: new [middleware/optionalAuth.js](middleware/optionalAuth.js) variant (cookie-aware, never 401s) +`publicReadLimiter`(240 req/min/IP); 7 GETs swapped from`authMiddleware` (`/api/games`, `/api/games/:gameId/comments`, `/api/leaderboard`, `/api/groups/discover`, `/api/groups/:groupId`with public-visibility gate,`/api/search`, `/api/users/:username/profile`with`friendStatus: null`stripping for anon). Services updated for null-viewer:`GroupService.discoverPublic`, `GroupService.getVisible`(anon sees public groups only — returns 404 for private to avoid leaking existence). Frontend: new [src/contexts/AuthGateContext.jsx](src/contexts/AuthGateContext.jsx) + [src/components/SignInModal.jsx](src/components/SignInModal.jsx) + [src/components/InlineGatePanel.jsx](src/components/InlineGatePanel.jsx).`showAuth`+`browseAsGuest`(persisted to`localStorage.sc_browse_as_guest`) lifted into AuthContext. App.jsx switch: `user → Dashboard; showAuth → AuthView; browseAsGuest → Dashboard; else → AuthView (Landing)`. DataContext gained `loadAnonDashboard()`(parallel fetch of public endpoints) — boot tries authed`loadDashboard`, falls back on 401. `performLogout`sets`browseAsGuest=true`so logout returns to anon mode, not the auth grid. Sidebar tabs filtered to Games/Groups/Rankings for anon. Top utility bar: anon shows`[Sign in]`+`[Sign up]`buttons (NotificationBell + UserMenu hidden). Action-gated components: GameCard (pick / undo), CommentThread (reactions + post replaced with InlineGatePanel), DashboardView (Join public group + Create group form replaced with InlineGatePanel + SearchBar onSelectGroup). FriendsList returns`null`for anon. ProfileView already anon-safe because backend returns`friendStatus: null`. Landing CTA "Or just browse as a guest →" + AuthView Back button switches to "Back to browsing" when `browseAsGuest`is set. All 8 specs (15 tests) green; no test changes required. **Marketing landing page (standalone, pre-Tier 11)** — [src/components/Landing.jsx](src/components/Landing.jsx) renders by default for unauthenticated visitors (hero with a giant glowing BANTRYX wordmark + tagline + dual CTAs, a 3-card stat strip, a 4-card feature grid, a 3-step how-it-works section, and a final CTA panel). [src/views/AuthView.jsx](src/views/AuthView.jsx) gates the existing login/register grid behind a`showAuth`flag; either Landing CTA flips it true. The flag's initial state reads`localStorage.sc_visited`(set by`markReturning()`inside AuthView's login/register/2FA wrappers) so a returning user — including one who just logged out — never sees the landing again in the same browser. A useEffect also pins`showAuth=true`whenever`authView`enters`forgot`/`reset`/`twofa`so deep-link flows like`/?resetToken=…`resolve to the auth grid on exit instead of dropping the user back on marketing copy. E2E impact: [tests/e2e/helpers/auth.js](tests/e2e/helpers/auth.js) gained a`dismissLanding(page)`helper that waits ≤5 s for the "Get started" CTA and clicks it before filling forms (no-op if the landing is bypassed); both`registerViaUI`and`loginViaUI`call it. All 8 specs (15 tests) green. **Sidebar nav + chrome cleanup (standalone, pre-Tier 11)** — left-column nav replaced the horizontal tab row; collapsible on desktop (240 px ↔ 64 px, persisted in`localStorage.sc_sidebar_collapsed`) and off-canvas drawer on mobile (< `md:`). The Bantryx eyebrow + tagline + "Predict & Win" panel were removed from `App.jsx`and`DashboardView.jsx`(modal title + ErrorBoundary text retained — situational, not top-of-screen). New components: [src/components/Sidebar.jsx](src/components/Sidebar.jsx), [src/components/UserMenu.jsx](src/components/UserMenu.jsx). SearchBar, NotificationBell, and the UserMenu (avatar → "View profile" / "Sign out") now live in a slim top utility bar at the top of`<main>`. AuthView wraps in a centered `max-w-5xl`container. E2E impact:`tests/e2e/helpers/auth.js`switched its post-login sentinel from the old`Logout`button to the always-present`getByRole('tab', { name: /Upcoming Matches/ })`; `logoutViaUI` now opens the UserMenu (`[aria-haspopup="menu"]`) and clicks the `Sign out` menuitem before confirming. Two leaderboard specs (`pick-and-result`, `leaderboard-scoring`) added `button:not([aria-haspopup])` to disambiguate the UserMenu trigger from leaderboard rows (both contain the username). All 8 specs (15 tests) green. **Tier 5.5b** — Playwright coverage expansion. Five new specs (`auth-security`, `friend-system`, `notifications-badges`, `leaderboard-scoring`, `admin-panel`) on top of the original three (`pick-and-result`, `group-lifecycle`, `comment-reaction`). All 8 specs run in CI on every PR. New shared helpers in [tests/e2e/helpers/api.js](tests/e2e/helpers/api.js) (HTTP `apiLogin`/`setGameResult`/`createPick`/`getLeaderboard`+ DB`resetUserLockout`/`insertPasswordResetToken`/`clearFriendships`/`clearPicksAndBadges`/`clearNotifications`/`clearGameResults`/`getUserId`) and [tests/e2e/helpers/admin.js](tests/e2e/helpers/admin.js) (`openAdminTab`). Covered invariants: Tier 6.6 lockout + Tier 6.4/6.8 password reset cascade + Tier 6.7 CSRF reject; admin GameManager CRUD + UserManager bulk role flip + Tier 5.3 cascade delete; friend request accept/decline/cancel; bell unread count + badge unlocks; probability-weighted scoring across 50/50, 60/40, 40/60 odds with Tier 5.2 cache invalidation. **Tier 13** — Codebase cleanup / modularization (six chunks). `server.js`2262 → 157 LOC (composition shell), 14 domain`routes/*.js`mounted at`/api`, 8 `services/\_.js`owning business logic.`src/App.jsx` 1308 → **71 LOC** (pure layout shell) — state + handlers live in three React Context providers (`AuthContext`/`DataContext`/`NotificationContext`); components consume via `useAuth`/`useData`/`useNotifications`/`useRequest` + selector hooks (`useGames`/`usePicks`/`useGroups`/`useLeaderboard`/`useFriends`). Chunk 5 migrated every component (GameCard, CommentThread, SearchBar, NotificationBell, FriendsList, ProfileDrawer, ProfileView, AdminPanel + children) off prop-drilling. Chunk 6 extracted `DashboardView`/`AuthView`/`SkeletonView`under`src/views/`. New lint rules: backend `no-console` + ban on deep relative imports. Pure refactor — no behavior change, Playwright 3/3 green on every chunk. Earlier: **Tier 5.5** — Playwright E2E suite (`pick-and-result`, `group-lifecycle`, `comment-reaction`specs) plus the matching CI job (cached Chromium, Postgres service, trace/report artifacts on failure). **Tier 9** — ESLint/Prettier/Husky baseline + frontend code-splitting + OpenAPI docs (dev) + Dockerfile + docker-compose + GitHub Actions CI (lint/build/migrations) + Bicep IaC for Azure (Log Analytics, App Insights, ACR, Key Vault, Postgres Flex, Container Apps env + main app + migrate Job) + GitHub Actions CD (OIDC federation, build → migrate → roll out) + custom domain`bantryx.com` with Azure managed TLS + Cloudflare DNS + www→apex redirect. App is live at https://bantryx.com. **Tier 6** — CORS allowlist + helmet headers + account lockout + per-route rate limits + email service + email verification + password reset + HttpOnly cookie auth + rotating refresh + CSRF + TOTP 2FA. **Tier 5 (core)** + **Tier 5.4b** — migrations framework + leaderboard cache + transactional cascades + structured logging + frontend error boundary + Sentry hook (opt-in). See [ARCHITECTURE.md §13](ARCHITECTURE.md) for full roadmap status.
